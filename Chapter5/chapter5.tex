\chapter{Kiểm thử và đánh giá}
\label{Chapter5}

Chương này mô tả môi trường, phương pháp và các kịch bản kiểm thử dùng để đánh giá CareerNova sau khi triển khai. Các kết quả về chức năng, hiệu năng và chất lượng thuật toán được phân tích nhằm đối chiếu với những yêu cầu đã xác định.

\section{Môi trường triển khai thử nghiệm hệ thống}

Để tiến hành thử nghiệm và đánh giá hệ thống CareerNova, phiên bản thử nghiệm được triển khai trên một Droplet của DigitalOcean tại khu vực Singapore (SGP1). Môi trường được triển khai theo kiến trúc container hóa; các dịch vụ giao tiếp nội bộ trên mạng Docker riêng, còn lưu lượng HTTP/HTTPS từ Internet được tiếp nhận qua lớp proxy.

\begin{itemize}
    \item \textbf{Cấu hình phần cứng máy chủ (Server Cloud)}:
    \begin{itemize}
        \item Hệ điều hành: Ubuntu 22.04 LTS x64.
        \item Máy chủ ảo: DigitalOcean Droplet \texttt{nova-vps} tại khu vực SGP1.
        \item Vi xử lý: 2 vCPU (Intel).
        \item Bộ nhớ RAM: 8 GB.
        \item Ổ cứng: 160 GB.
    \end{itemize}
    \item \textbf{Môi trường ảo hóa và container}:
    \begin{itemize}
        \item Docker Compose dùng để điều phối các dịch vụ Frontend (Next.js), Backend API (NestJS), ETL Pipeline (FastAPI), PostgreSQL và Nginx Proxy Manager.
        \item Cụm dịch vụ triển khai thực tế gồm \texttt{nova-frontend}, \texttt{nova-backend}, \texttt{nova-algo-api}, \texttt{nova-postgres} và \texttt{nginx-proxy-manager}.
    \end{itemize}
    \item \textbf{Thông số cấu hình mô hình AI}:
    \begin{itemize}
        \item Mô hình tạo vector nhúng: \texttt{all-MiniLM-L6-v2} (chạy trực tiếp trên CPU thông qua thư viện PyTorch).
        \item Trích xuất dữ liệu: Sử dụng mô hình \texttt{gemini-2.5-flash} thông qua API.
    \end{itemize}
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{server_specs_terminal.png}
    \caption{Bằng chứng thô của môi trường triển khai: kết quả các lệnh \texttt{lsb\_release}, \texttt{free -h}, \texttt{df -h} và \texttt{lscpu} thực thi trực tiếp trên máy chủ \texttt{nova-vps}}
    \label{fig:server_specs}
\end{figure}

Hình~\ref{fig:server_specs} chứng minh cấu hình phần cứng và hệ điều hành nêu trên là thông số thực tế của máy chủ đang vận hành chứ không phải giá trị khai báo: hệ điều hành Ubuntu 22.04.5 LTS, 2 vCPU, 7,8~GiB RAM và ổ đĩa 155~GB (trên tổng dung lượng Droplet 160~GB). Đây là mốc cấu hình dùng để diễn giải mọi số liệu hiệu năng ở Mục 5.3, đặc biệt là chi phí tính vector nhúng khá cao do máy chủ chỉ có 2 nhân xử lý và không trang bị GPU.

Việc đánh giá được tổ chức theo ba lớp độc lập để tránh suy diễn từ một loại bằng chứng sang loại kết luận khác. Kiểm thử chức năng trả lời hệ thống có thực hiện đúng hành vi đã đặc tả hay không; đo hiệu năng phản ánh độ trễ thực tế trên môi trường triển khai; còn đánh giá thuật toán xem xét chất lượng đầu ra so với nhãn chuẩn hoặc các trường hợp đối soát. Các số liệu đều được gắn với thời điểm đo và cấu hình ở Mục 5.1 để bảo đảm có thể lặp lại.

\begin{table}[H]
\caption{Khung liên kết giữa mục tiêu đánh giá, phương pháp và bằng chứng}
\label{tab:evaluation_framework}
\begin{center}
\begin{tabular}{|L{3.2cm}|L{4.2cm}|L{5.8cm}|}
\hline
\textbf{Mục tiêu} & \textbf{Phương pháp} & \textbf{Bằng chứng và cách diễn giải} \\
\hline
Chức năng hoạt động đúng & Kiểm thử hộp đen theo hành trình người dùng & Kịch bản, đầu vào, kết quả mong đợi và trạng thái đạt/không đạt (Mục 5.2). \\
\hline
Phản hồi đủ nhanh & Đo trường \textit{Duration} trong Chrome DevTools Network, lặp lại theo từng API đại diện & Trung bình, P95, lớn nhất và tỷ lệ lỗi; phân biệt cache miss và cache hit (Mục 5.3). \\
\hline
Đầu ra thuật toán hợp lý & Đối chiếu nhãn thủ công (Ground Truth) do nhóm gán trên từng tập mẫu & Precision, Recall, F1 cho NER; độ chính xác chuẩn hóa/lọc nhiễu; Precision/Recall/F1 cho khử trùng lặp; và Precision@K, MRR cho gợi ý việc làm (Mục 5.4). \\
\hline
\end{tabular}
\end{center}
\end{table}

\section{Kiểm thử chức năng hệ thống}

\subsection{Ma trận kịch bản kiểm thử (Test Matrix)}

Hệ thống được kiểm thử thông qua các kịch bản kiểm thử hộp đen (Black-box Testing) để đảm bảo các tính năng hoạt động đúng đặc tả yêu cầu. Các kịch bản được xây dựng theo hành trình sử dụng thực tế của vai trò người dùng sinh viên/ứng viên, từ lúc tạo tài khoản, quản lý hồ sơ CV, phân tích đối soát đến khai thác dữ liệu thị trường, và bám sát các chức năng đang có trên hệ thống. Bảng~\ref{tab:test_cases} liệt kê 11 ca kiểm thử cùng dữ liệu đầu vào và kết quả mong đợi tương ứng.

\begin{longtable}{|c|L{3.0cm}|L{3.5cm}|L{3.5cm}|C{1.5cm}|}
\caption{Kịch bản kiểm thử chức năng hệ thống}
\label{tab:test_cases} \\
\hline
\textbf{ID} & \textbf{Chức năng} & \textbf{Dữ liệu đầu vào / Điều kiện} & \textbf{Kết quả mong đợi} & \textbf{Trạng thái} \\
\hline
\endfirsthead
\multicolumn{5}{c}%
{{ \tablename\ \thetable{} -- tiếp tục từ trang trước}} \\
\hline
\textbf{ID} & \textbf{Chức năng} & \textbf{Dữ liệu đầu vào / Điều kiện} & \textbf{Kết quả mong đợi} & \textbf{Trạng thái} \\
\hline
\endhead
\hline \multicolumn{5}{|r|}{{Tiếp tục ở trang sau}} \\ \hline
\endfoot
\hline
\endlastfoot
TC-01 & Đăng ký tài khoản mới & Điền đúng Email, Tên, Mật khẩu có độ dài $\ge$ 6 ký tự & Tạo tài khoản thành công, lưu thông tin vào PostgreSQL & ĐẠT \\
\hline
TC-02 & Đăng nhập hệ thống & Nhập đúng Email và Mật khẩu & Nhận JWT Token, chuyển hướng tới trang Dashboard & ĐẠT \\
\hline
TC-03 & Quên mật khẩu & Nhập đúng email đã đăng ký & Gửi email đặt lại mật khẩu và chuyển tới trang xác nhận & ĐẠT \\
\hline
TC-04 & Xác thực email & Mở liên kết xác thực từ email & Tài khoản được xác thực và chuyển hướng đăng nhập thành công & ĐẠT \\
\hline
TC-05 & Tải lên CV hợp lệ & File CV định dạng PDF/JPG/JPEG/PNG, dung lượng $\le$ 5 MB & Tải lên thành công và xuất hiện trong danh sách CV của người dùng & ĐẠT \\
\hline
TC-06 & Từ chối file sai định dạng hoặc quá dung lượng & File DOCX/TXT hoặc file ảnh vượt quá 5 MB & Hệ thống trả về lỗi hợp lệ và không lưu file & ĐẠT \\
\hline
TC-07 & Quản lý CV mặc định và xóa CV & Chọn một CV khác làm mặc định hoặc xóa một CV đã lưu & CV mặc định được cập nhật hoặc CV bị xóa khỏi danh sách & ĐẠT \\
\hline
TC-08 & Đối soát CV và tính Skill Gap & Chọn CV và nhấn đối soát với nhóm nghề & Hiển thị phần trăm tương thích, radar kỹ năng và danh sách kỹ năng thiếu & ĐẠT \\
\hline
TC-09 & Xem Dashboard thống kê thị trường việc làm & Truy cập trang Dashboard và chọn bộ lọc khu vực/thời gian/loại hình & Hiển thị đúng xu hướng, phân bổ thị trường và chỉ số tổng quan theo bộ lọc & ĐẠT \\
\hline
TC-10 & Tìm kiếm việc làm & Nhập từ khóa và bộ lọc cấp độ kinh nghiệm/thời gian phù hợp & Trả về danh sách việc làm đúng tiêu chí tìm kiếm & ĐẠT \\
\hline
TC-11 & Lưu và hủy lưu việc làm & Bấm lưu trên một tin tuyển dụng và sau đó hủy lưu & Công việc được thêm vào và xóa khỏi danh sách đã lưu tương ứng & ĐẠT \\
\hline
\end{longtable}

Mười một kịch bản trong Bảng~\ref{tab:test_cases} bao phủ bốn nhóm chức năng theo trình tự trải nghiệm người dùng: nhóm xác thực tài khoản (TC-01 đến TC-04), nhóm quản lý hồ sơ CV (TC-05 đến TC-07), nhóm phân tích đối soát năng lực (TC-08) và nhóm khai thác dữ liệu thị trường (TC-09 đến TC-11). Trong đó, TC-06 được thiết kế riêng để kiểm tra biên từ chối dữ liệu không hợp lệ (định dạng và dung lượng tệp), còn TC-08 là kịch bản phức tạp nhất vì đi xuyên qua toàn bộ chuỗi xử lý AI của hệ thống.

\clearpage
\subsection{Kết quả thực thi kiểm thử}

Toàn bộ các kịch bản kiểm thử trên đã được thực thi trên môi trường chạy thật tại địa chỉ \texttt{career-nova.online}. Kết quả từng ca được tổng hợp trong Bảng~\ref{tab:test_results} theo dạng đạt/không đạt để phản ánh nhanh mức độ hoàn thành chức năng:

\begin{longtable}{|C{1.8cm}|L{6.0cm}|L{4.2cm}|C{2.0cm}|}
\caption{Kết quả thực thi kiểm thử chức năng}\label{tab:test_results} \\
\hline
\textbf{Mã KB} & \textbf{Chức năng kiểm thử} & \textbf{Trạng thái thực tế} & \textbf{Kết quả} \\
\hline
\endfirsthead
\multicolumn{4}{c}{\tablename~\thetable\ -- tiếp tục từ trang trước} \\
\hline
\textbf{Mã KB} & \textbf{Chức năng kiểm thử} & \textbf{Trạng thái thực tế} & \textbf{Kết quả} \\
\hline
\endhead
TC-01 & Đăng ký tài khoản mới & Hoạt động bình thường & ĐẠT \\
\hline
TC-02 & Đăng nhập hệ thống & Đăng nhập và lưu token thành công & ĐẠT \\
\hline
TC-03 & Quên mật khẩu & Email đặt lại mật khẩu được gửi và truy cập đúng trang xác nhận & ĐẠT \\
\hline
TC-04 & Xác thực email & Liên kết xác thực hoạt động đúng và tài khoản được kích hoạt & ĐẠT \\
\hline
TC-05 & Tải lên CV hợp lệ & File được upload thành công và xuất hiện trong danh sách & ĐẠT \\
\hline
TC-06 & Từ chối file sai định dạng hoặc quá dung lượng & Hệ thống trả lỗi đúng quy định và không nhận file & ĐẠT \\
\hline
TC-07 & Quản lý CV mặc định và xóa CV & Đặt CV mặc định mới và xóa CV cũ thành công & ĐẠT \\
\hline
TC-08 & Đối soát CV và tính Skill Gap & Trả về điểm tương thích và gợi ý kỹ năng hợp lý & ĐẠT \\
\hline
TC-09 & Xem Dashboard thống kê thị trường việc làm & Biểu đồ và bộ lọc hiển thị đúng theo dữ liệu hiện có & ĐẠT \\
\hline
TC-10 & Tìm kiếm việc làm & Kết quả tìm kiếm đúng với từ khóa và bộ lọc & ĐẠT \\
\hline
TC-11 & Lưu và hủy lưu việc làm & Danh sách đã lưu cập nhật đúng khi thêm/xóa & ĐẠT \\
\hline
\end{longtable}

Theo Bảng~\ref{tab:test_results}, toàn bộ \textbf{11/11 kịch bản đạt (100\%)}, xác nhận các chức năng cốt lõi vận hành đúng đặc tả trên môi trường triển khai thật. Đáng chú ý, hai ca có rủi ro cao nhất đều vượt qua: TC-06 chứng minh tầng kiểm tra dữ liệu đầu vào từ chối đúng các tệp sai định dạng hoặc vượt 5 MB mà không để lọt dữ liệu rác vào hệ thống lưu trữ, và TC-08 xác nhận chuỗi xử lý AI nhiều bước gồm trích xuất, chuẩn hóa và so khớp trả về kết quả nhất quán từ đầu đến cuối. Kiểm thử chức năng vì vậy chỉ trả lời câu hỏi ``hệ thống có chạy đúng hay không''; các khía cạnh định lượng về tốc độ và độ chính xác được đo riêng tại Mục 5.3 và 5.4.

Ngoài trạng thái đạt/không đạt tổng hợp ở Bảng~\ref{tab:test_results}, phần này ghi nhận kết quả thực thi chi tiết của các luồng có nhiều thành phần nhất trong ma trận. Giao diện tương ứng của những chức năng này đã được trình bày ở Chương~4, nên ở đây chỉ tổng hợp \textit{kết quả kiểm thử} mà không lặp lại ảnh chụp màn hình. Các số liệu biến động theo thời gian (số tin tuyển dụng, điểm khớp và danh sách kỹ năng) được diễn giải theo snapshot tại thời điểm kiểm thử, không được coi là hằng số của hệ thống.

\begin{itemize}
    \item \textbf{TC-08 --- Đối soát CV và tính Skill Gap}: chuỗi xử lý AI vận hành đúng đầu--cuối; hệ thống không chỉ trả về một điểm tương thích tổng hợp mà còn phân rã thành nhóm kỹ năng đã đạt, nhóm kỹ năng cần bổ sung và biểu đồ radar so sánh trực quan với yêu cầu công việc. Kết quả này khẳng định đầu ra của Matching Engine có cấu trúc rõ ràng và có thể diễn giải được cho người dùng, thay vì chỉ là một con số đơn lẻ.
    \item \textbf{TC-09 --- Dashboard thị trường}: tầng trực quan hóa tổng hợp và hiển thị đúng dữ liệu thị trường theo bộ lọc người dùng chọn; các chỉ số tổng quan, biểu đồ xu hướng tuyển dụng và phân bố nhóm kỹ năng được kết xuất đồng thời trên cùng một màn hình. Kết quả này xác nhận sáu API Dashboard trả về dữ liệu hợp lệ và tầng Frontend ghép nối chính xác thành một bức tranh thị trường có ý nghĩa hỗ trợ ra quyết định.
    \item \textbf{TC-06 --- Từ chối tệp không hợp lệ}: tầng kiểm tra dữ liệu đầu vào chặn đúng tại biên; tệp vượt quá 5~MB bị từ chối kèm thông báo giới hạn rõ ràng và không được ghi vào kho lưu trữ. Đây là bằng chứng cho thấy hệ thống bảo vệ được tính toàn vẹn của dữ liệu, ngăn tệp rác hoặc quá khổ đi vào pipeline xử lý phía sau.
    \item \textbf{TC-10 --- Tìm kiếm việc làm}: chức năng áp đúng tiêu chí lọc trên tập dữ liệu thực; với từ khóa ``business analyst'', danh sách trả về chỉ gồm các tin phù hợp với truy vấn. Kết quả này xác nhận truy vấn lọc phía Backend hoạt động chính xác, giúp người dùng thu hẹp đúng phạm vi việc làm quan tâm.
    \item \textbf{TC-11 --- Lưu và hủy lưu việc làm}: luồng lưu trạng thái cá nhân hóa hoạt động bền vững; tin được lưu xuất hiện đúng trong danh sách ``Cơ hội đã lưu'' gắn với tài khoản người dùng. Bằng chứng này khẳng định thao tác ghi và truy xuất dữ liệu theo từng người dùng diễn ra chính xác, làm nền tảng cho các tính năng cá nhân hóa của hệ thống.
\end{itemize}

\section{Đánh giá hiệu năng và dữ liệu thực tế}

\subsection{Đánh giá khả năng trực quan hóa và hiển thị dữ liệu trên Dashboard}

Số lượng dữ liệu tuyển dụng thực tế trong cơ sở dữ liệu liên tục biến động theo lịch đồng bộ từ các nguồn Crawler. Do đó, để phản ánh trung thực quy mô dữ liệu tại một mốc xác định, báo cáo sử dụng một ảnh chụp trạng thái (snapshot) được \textbf{truy vấn trực tiếp từ cơ sở dữ liệu} tại thời điểm kiểm thử (14/07/2026). Các con số trong Bảng~\ref{tab:data_stats} là kết quả đếm chính xác (\texttt{count(*)}) trên các bảng gốc, kèm bằng chứng thô ở Hình~\ref{fig:db_snapshot_psql}:

\begin{table}[H]
\caption{Số liệu tổng hợp truy vấn trực tiếp cơ sở dữ liệu tại thời điểm kiểm thử (14/07/2026)}
\label{tab:data_stats}
\begin{center}
\begin{tabular}{|L{6.0cm}|C{3.0cm}|L{5.0cm}|}
\hline
\textbf{Chỉ số (truy vấn CSDL)} & \textbf{Giá trị} & \textbf{Ghi chú} \\
\hline
Tổng số tin tuyển dụng đã lưu & 7.245 & Số bản ghi sạch tích lũy trong bảng \texttt{jobs} sau khử trùng lặp (nguồn ITViec, VietnamWorks, v.v.) \\
\hline
Tin còn hiệu lực (chưa hết hạn) & 6.278 & Số tin có \texttt{expiry\_time} rỗng hoặc còn trong tương lai \\
\hline
Số công ty & 3.511 & Số bản ghi trong bảng \texttt{companies} sau chuẩn hóa và gộp nhóm \\
\hline
Số kỹ năng chuẩn hóa & 6.048 & Số bản ghi trong từ điển kỹ năng master (\texttt{skills}) \\
\hline
Thời điểm truy vấn & 14/07/2026 & Ảnh chụp trạng thái CSDL tại thời điểm đo (xem Hình~\ref{fig:db_snapshot_psql}) \\
\hline
\end{tabular}
\end{center}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{db_snapshot_psql.png}
    \caption{Bằng chứng thô cho Bảng~\ref{tab:data_stats}: kết quả truy vấn \texttt{count(*)} trực tiếp trên cơ sở dữ liệu production (\texttt{nova-postgres}) tại thời điểm 14/07/2026}
    \label{fig:db_snapshot_psql}
\end{figure}

Hình~\ref{fig:db_snapshot_psql} chứng minh các con số quy mô dữ liệu trong Bảng~\ref{tab:data_stats} là kết quả đếm chính xác trực tiếp trên cơ sở dữ liệu tại một thời điểm xác định, không phải giá trị ước lượng hay số liệu tổng hợp lấy từ giao diện. Việc gắn ảnh chụp truy vấn kèm mốc thời gian bảo đảm tính minh bạch và khả năng kiểm chứng của số liệu, phù hợp với đặc thù dữ liệu luôn tăng theo lịch đồng bộ của hệ thống.

Sự chênh lệch giữa tổng số tin tích lũy (7.245) và số tin người dùng nhìn thấy trên Dashboard phản ánh đúng đặc thù luồng xử lý của hệ thống: các tin hết hạn nộp hồ sơ vẫn tồn tại trong cơ sở dữ liệu gốc để phục vụ phân tích xu hướng dài hạn (tính trọng số TF-IDF, gợi ý kỹ năng), trong khi tầng Frontend (Dashboard) chỉ truy vấn và trực quan hóa các tin còn hiệu lực và áp thêm bộ lọc thời gian (mặc định 30 ngày, tương ứng kết quả kiểm thử TC-09). Việc bóc tách rõ ràng giữa dữ liệu thô lưu trữ ở Backend và dữ liệu tổng hợp hiển thị ở Frontend giúp hệ thống duy trì hiệu năng cao khi phản hồi các yêu cầu từ phía người dùng.

\subsection{Đo lường thời gian phản hồi (Response Time) và đánh giá độ trễ}

Để đánh giá thời gian phản hồi một cách nhất quán và dễ lặp lại, hệ thống được kiểm thử theo 20 lượt cho mỗi API đại diện trên môi trường triển khai thật. Thời gian được lấy từ trường \textit{Duration} của yêu cầu trong tab \textit{Network} của trình duyệt, tính từ lúc trình duyệt gửi yêu cầu đến khi nhận đủ phản hồi HTTP. Cách đo này loại trừ sai số do thao tác bấm giờ thủ công và phản ánh trực tiếp độ trễ của dịch vụ.

\begin{itemize}
    \item \textbf{Dashboard}: sử dụng endpoint có độ trễ lớn nhất trong sáu API dữ liệu Dashboard là \texttt{GET /dashboard/skills/rising}, với bộ lọc 30 ngày gần nhất.
    \item \textbf{Upload CV}: sử dụng yêu cầu \texttt{POST /cv/upload} với cùng một tệp PDF hợp lệ (CV mẫu kỹ sư phần mềm, 1 trang) trong cả 20 lượt đo.
    \item \textbf{Đối soát CV}: sử dụng yêu cầu \texttt{POST /matching/analyze} với cùng nhóm nghề mục tiêu (\textit{software engineer}). Để tách bạch hai chế độ hoạt động của hệ thống, phép đo gồm 20 lượt phân tích lần đầu trên 20 bản CV tải lên độc lập (buộc thực thi toàn bộ pipeline gồm trích xuất kỹ năng qua Gemini API, tương ứng cache miss) và 20 lượt phân tích lặp lại trên cùng một CV (tái sử dụng kỹ năng đã trích xuất, tương ứng cache hit).
\end{itemize}

Với mỗi chức năng, các chỉ số tổng hợp gồm: số lần gọi, thời gian trung bình, P95, thời gian lớn nhất và tỷ lệ lỗi. Mỗi chức năng được đo 20 lượt để chỉ số P95 (bách phân vị thứ 95) có ý nghĩa đại diện; riêng phép đo cache hit cũng gồm 20 lượt phân tích lặp lại trên cùng một CV nhằm minh họa tác động của cơ chế cache. Do dịch vụ so khớp chạy trên máy chủ chỉ có 2 vCPU không GPU, các lượt đo cache miss được thực hiện tuần tự và có giãn cách hợp lý giữa các lần gọi để phản ánh đúng độ trễ của một phiên phân tích đơn lẻ, tránh hiện tượng nghẽn do gọi dồn đồng thời.

Trước hết, sáu endpoint dữ liệu của Dashboard thị trường được đo trực tiếp trên môi trường triển khai thật (\texttt{career-nova.online}, máy chủ DigitalOcean SGP1) bằng 20 lượt gọi HTTPS liên tiếp cho mỗi endpoint với bộ lọc 30 ngày. Kết quả được trình bày trong Bảng~\ref{tab:api_performance_dashboard}.

\begin{table}[H]
\caption{Thời gian phản hồi thực đo của sáu API Dashboard thị trường (tháng 07/2026)}
\label{tab:api_performance_dashboard}
\begin{center}
\begin{tabular}{|L{4.2cm}|C{1.4cm}|C{1.8cm}|C{1.8cm}|C{1.8cm}|C{1.4cm}|}
\hline
\textbf{Endpoint} & \textbf{Số lần gọi} & \textbf{Trung bình (ms)} & \textbf{P95 (ms)} & \textbf{Lớn nhất (ms)} & \textbf{Tỷ lệ lỗi} \\
\hline
\texttt{/dashboard/} \newline \texttt{stats} & 20 & 260 & 307 & 331 & 0\% \\
\hline
\texttt{/dashboard/} \newline \texttt{trends} & 20 & 207 & 241 & 242 & 0\% \\
\hline
\texttt{/dashboard/} \newline \texttt{industries} & 20 & 486 & 522 & 566 & 0\% \\
\hline
\texttt{/dashboard/} \newline \texttt{hot-jobs} & 20 & 297 & 410 & 418 & 0\% \\
\hline
\texttt{/dashboard/} \newline \texttt{skills/in-demand} & 20 & 975 & 1113 & 1122 & 0\% \\
\hline
\texttt{/dashboard/} \newline \texttt{skills/rising} & 20 & 1327 & 1588 & 1613 & 0\% \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả cho thấy toàn bộ 120 lượt gọi đều thành công (tỷ lệ lỗi \textbf{0\%}). Bốn trong sáu endpoint phản hồi trung bình dưới 0,5 giây; riêng hai endpoint nhóm kỹ năng nặng hơn với thời gian trung bình lần lượt 0,98 giây (\texttt{in-demand}) và 1,33 giây (\texttt{rising}). Do Frontend gửi sáu truy vấn song song bằng \texttt{Promise.all}, thời gian chờ dữ liệu của toàn trang Dashboard về nguyên tắc bị chi phối bởi endpoint chậm nhất (\texttt{skills/rising}, trung bình \textbf{1.327ms}), thay vì tổng thời gian sáu truy vấn tuần tự (khoảng 3,6 giây). Với P95 lớn nhất trong sáu endpoint là 1,59 giây (\texttt{skills/rising}), toàn bộ các API Dashboard đều thỏa NFR-01 (thời gian phản hồi $\le 2$ giây cho 95\% yêu cầu) ở mức đo API. Hai endpoint nhóm kỹ năng (\texttt{in-demand} và \texttt{rising}) chậm hơn đáng kể so với nhóm còn lại vì phải thực hiện truy vấn tổng hợp (aggregation) trên bảng liên kết \texttt{job\_skills} có số bản ghi lớn nhất; đây là điểm tối ưu tiềm năng bằng chỉ mục bổ sung hoặc materialized view trong tương lai.

Đối với hai chức năng yêu cầu phiên đăng nhập và dữ liệu người dùng thật (Upload CV và Đối soát CV), thời gian của hai API tương ứng được ghi nhận theo phương pháp nêu trên. Kết quả tổng hợp được trình bày trong Bảng~\ref{tab:api_performance_auth}.

\begin{table}[H]
\caption{Bảng tổng hợp thời gian phản hồi theo thao tác người dùng (tháng 07/2026)}
\label{tab:api_performance_auth}
\begin{center}
\begin{tabular}{|L{3.5cm}|C{1.4cm}|C{1.8cm}|C{1.8cm}|C{1.8cm}|C{1.4cm}|}
\hline
\textbf{Chức năng} & \textbf{Số lần đo} & \textbf{Trung bình (s)} & \textbf{P95 (s)} & \textbf{Lớn nhất (s)} & \textbf{Tỷ lệ lỗi} \\
\hline
Dashboard \newline (API \texttt{skills/rising}) & 20 & 1,327 & 1,588 & 1,613 & 0\% \\
\hline
Upload CV & 20 & 1,31 & 2,03 & 2,63 & 0\% \\
\hline
Đối soát CV (lần đầu, cache miss) & 20 & 41,9 & 49,3 & 55,1 & 0\% \\
\hline
Đối soát CV (lặp lại, cache hit) & 20 & 0,27 & 0,31 & 0,31 & 0\% \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả đo cho thấy sự phân hóa rõ rệt giữa hai chế độ hoạt động của chức năng Đối soát CV. Ở lượt phân tích lần đầu (cache miss), toàn bộ pipeline được thực thi tuần tự: tải văn bản CV, gọi Gemini API trích xuất kỹ năng, chuẩn hóa qua FAISS trên CPU và tính điểm so khớp, dẫn đến thời gian trung bình \textbf{41,9 giây} (P95 49,3 giây, lớn nhất 55,1 giây), \textbf{đạt ngưỡng yêu cầu 60 giây của NFR-02}. Đồng thời, toàn bộ 20 lượt đều trả về kết quả thành công và điểm số ổn định (khoảng 0,57). Nguyên nhân chính khiến lượt gọi đầu mất thời gian là độ trễ của Gemini API bên ngoài cộng với chi phí tính vector nhúng trên máy chủ chỉ có 2 vCPU không GPU. Ngược lại, các lượt phân tích lặp lại trên cùng CV (cache hit) chỉ mất trung bình \textbf{0,27 giây}, nhanh hơn khoảng 155 lần do kỹ năng đã trích xuất được tái sử dụng từ bảng \texttt{user\_cv\_skills}. Như vậy NFR-02 đã được thỏa mãn cho cả hai chế độ. Cần nhấn mạnh rằng toàn bộ số liệu hiệu năng ở mục này được đo theo chế độ \textit{đơn phiên} (một người dùng, các lượt gọi tuần tự và có giãn cách hợp lý), nên chỉ phản ánh độ trễ của một phiên phân tích riêng lẻ, \textbf{chưa phản ánh hành vi khi nhiều người dùng truy cập đồng thời}. Với hạ tầng chỉ 2 vCPU không GPU, khi số phiên đối soát đồng thời tăng lên, thời gian đối soát lần đầu (cache miss) có nguy cơ vượt ngưỡng 60 giây của NFR-02 do tranh chấp tài nguyên CPU khi tính vector nhúng. Do đó, kiểm thử tải (load/stress test) và đánh giá khả năng mở rộng theo chiều ngang chưa được thực hiện trong phạm vi đề tài và được nhìn nhận là một hạn chế ở Mục~6.2. Dù vậy, ngay cả ở chế độ đơn phiên, trải nghiệm lần phân tích đầu tiên vẫn có thể được nâng cao thông qua các giải pháp như: trích xuất kỹ năng bất đồng bộ ngay sau khi upload CV (thay vì chờ đến lúc người dùng bấm phân tích), hiển thị tiến trình từng bước trên giao diện, hoặc nâng cấp hạ tầng suy luận vector nhúng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{perf_measurement_terminal.png}
    \caption{Bằng chứng thô của phép đo hiệu năng: nhật ký terminal ghi lại trường \textit{Duration} (thời gian phản hồi) của từng lượt gọi API trên môi trường triển khai thật, là dữ liệu nguồn để tổng hợp Bảng~\ref{tab:api_performance_dashboard} và Bảng~\ref{tab:api_performance_auth}}
    \label{fig:perf_measurement_terminal}
\end{figure}

Hình~\ref{fig:perf_measurement_terminal} chứng minh các số liệu trong hai bảng trên là kết quả đo thực tế chứ không phải ước lượng: mỗi lượt gọi API được ghi trực tiếp trường \textit{Duration} bằng công cụ đo (\texttt{curl -w \%\{time\_total\}} và tab \textit{Network} của trình duyệt), qua đó bảo đảm tính tái lập của phép đo. Ảnh cũng làm rõ hai chế độ vận hành của chức năng đối soát, gồm cache miss (hàng chục giây) và cache hit (dưới một giây), đúng như phân tích ở trên, đồng thời xác nhận toàn bộ các lượt đo đều thành công (tỷ lệ lỗi 0/20).

\section{Đánh giá thuật toán}
\label{sec:algorithm_evaluation}

Nhằm kiểm chứng tính đúng đắn và độ chính xác của các thuật toán xử lý dữ liệu trong hệ thống, bao gồm bóc tách dữ liệu bằng LLM, chuẩn hóa kỹ năng hai tầng (khớp chuỗi và tìm kiếm ngữ nghĩa trên embedding), gộp nhóm doanh nghiệp bằng chuẩn hóa hậu tố pháp lý, khử trùng lặp sâu bài đăng bằng vector nhúng kết hợp Cosine Similarity và thuật toán so khớp Matching Engine, chúng tôi xây dựng bộ kịch bản đối soát thực tế và khung chỉ số đánh giá hiệu năng. % ==========================================================================
% GHI CHÚ PHẢN BIỆN (A2 - ĐÃ KHỚP SỐ THẬT, xong):
%   Cỡ mẫu đã được khớp với artifact trong KiemThu/: NER 20 JD + 20 CV; chuẩn hóa 758 mẫu
%   (312 mapping + 446 reject) + 24 DN; dedup 17 tin/52 cặp; gợi ý việc làm 10 CV.
%   Câu "100 bài đăng (50 CV + 50 JD)" đã được thay ở mở đầu 5.4, Mục 5.4.2 và Chương 1.
%   (Tùy chọn) Có thể thêm 1 bảng "Tổng quan bộ dữ liệu đánh giá" liệt kê gọn 4 cỡ mẫu trên.
% GHI CHÚ PHẢN BIỆN (A4 - THIẾU BASELINE, còn tồn):
%   Bổ sung tối thiểu một bảng ablation: (i) chỉ khớp từ khóa, (ii) SBERT không TF-IDF,
%   (iii) SBERT + TF-IDF + 6 luật lọc (đầy đủ) — trên cùng tập nhãn (20 JD + 20 CV).
%   Đây là bằng chứng định lượng cho tuyên bố "giải pháp lai tốt hơn" ở Chương 1-2.
% ==========================================================================
Mỗi kịch bản kiểm thử sử dụng tập dữ liệu gán nhãn riêng phù hợp với thuật toán tương ứng, đều thu thập thực tế từ thị trường công nghệ thông tin tại Việt Nam: trích xuất thực thể (NER) trên 20 JD và 20 CV; chuẩn hóa kỹ năng trên 758 mẫu kỹ năng thô và 24 doanh nghiệp; khử trùng lặp trên 17 tin (52 cặp đối soát); và gợi ý việc làm trên 10 CV mẫu (chi tiết cỡ mẫu của từng kịch bản được nêu tại mục tương ứng). Với mỗi thuật toán, chúng tôi trình bày kết quả thực đo trên các trường hợp đối soát cụ thể (so sánh trực quan dữ liệu đầu vào và đầu ra) kèm khung tiêu chí (ngưỡng chấp nhận) mà thuật toán được thiết kế để đạt được; riêng bộ chỉ số trích xuất kỹ năng (NER) đã được định lượng đầy đủ trên tập mẫu.

\subsection{Đánh giá độ chính xác của định dạng dữ liệu trả về từ mô hình và các thuật toán tiền xử lý}

Phần này trình bày các kịch bản đối soát thực tế và khung chỉ số đánh giá chất lượng cho các thuật toán xử lý dữ liệu đầu vào, bao gồm bóc tách dữ liệu bán cấu trúc bằng mô hình ngôn ngữ lớn (LLM), chuẩn hóa kỹ năng hai tầng, gộp nhóm doanh nghiệp và khử trùng sâu tin đăng bằng vector nhúng kết hợp Cosine Similarity. Các thực nghiệm dưới đây được thực thi trực tiếp trên môi trường triển khai thật (container \texttt{nova-algo-api} và cơ sở dữ liệu production, tháng 07/2026).

\subsubsection*{1. Kịch bản đối soát 1: Trích xuất thực thể và Structured Output từ CV/JD thô}

Mục tiêu của kịch bản này là đánh giá chất lượng của module trích xuất thông tin kỹ năng qua Gemini API với cấu hình nhiệt độ phát sinh $\text{temperature} = 0.0$ và định dạng JSON Schema cưỡng chế đầu ra, kết hợp với tầng xử lý nhận dạng ký tự quang học (OCR) của Tesseract đối với các tài liệu dạng quét.

Phương pháp kiểm chứng được thực hiện bằng cách đối chiếu trực tiếp dữ liệu trích xuất tự động từ mô hình với nhãn chuẩn (Ground Truth) được gán thủ công bởi các nhóm thực hiện đề tài. Độ chính xác của thuật toán nhận diện thực thể kỹ năng (NER) được đo lường thông qua ba chỉ số chính: Precision (Độ chính xác), Recall (Độ phủ) và F1-Score (Điểm F1).

\begin{equation}
\text{Precision} = \frac{\text{TP}}{\text{TP} + \text{FP}}
\end{equation}
\begin{equation}
\text{Recall} = \frac{\text{TP}}{\text{TP} + \text{FN}}
\end{equation}
\begin{equation}
\text{F1-Score} = 2 \times \frac{\text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}
\end{equation}

Trong đó:
\begin{itemize}
    \item $\text{TP}$ (True Positives): Số kỹ năng trích xuất chính xác và có bằng chứng xác thực trong văn bản gốc.
    \item $\text{FP}$ (False Positives): Số kỹ năng bị trích xuất sai hoặc không xuất hiện trong văn bản gốc (ảo giác dữ liệu).
    \item $\text{FN}$ (False Negatives): Số kỹ năng thực tế có trong văn bản nhưng mô hình bỏ sót.
\end{itemize}

Quy trình nạp dữ liệu đối soát (20 bản ghi JD, 20 bản ghi CV và tập nhãn chuẩn tương ứng) phục vụ kiểm chứng chéo được minh họa qua Hình~\ref{fig:kb1_load}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{image_KB1/cell3_KB1.png}
    \caption{Kết quả thực thi ô nạp dữ liệu đối soát NER: 20 bản ghi JD, 20 bản ghi CV và tập nhãn chuẩn (Ground Truth) tương ứng.}
    \label{fig:kb1_load}
\end{figure}

Dưới đây là biểu mẫu đối soát cấu trúc dữ liệu kỹ năng được trích xuất từ CV/JD mẫu:






Bảng \ref{tab:match_extraction_details} cung cấp khung đối soát trực quan giữa nhãn thô và thực thể bóc tách tương ứng kèm theo bằng chứng chuỗi ký tự tự động từ mô hình LLM. Qua bảng này, đánh giá khả năng hiểu ngữ cảnh văn bản và mức độ trung thực của thông tin trích xuất, triệt tiêu khả năng phát sinh ảo giác của mô hình ngôn ngữ lớn.

\begin{table}[H]
\caption{Bảng đối soát thống kê kết quả thực nghiệm trích xuất thực thể kỹ năng (NER) theo tập dữ liệu}
\label{tab:match_extraction_details}
\begin{center}
\begin{tabular}{|L{2.4cm}|C{1.2cm}|C{1.0cm}|C{1.0cm}|C{1.0cm}|C{1.5cm}|C{1.5cm}|C{1.5cm}|}
\hline
\textbf{Tập dữ liệu} & \textbf{Số lượng} & \textbf{TP} & \textbf{FP} & \textbf{FN} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
\hline
Tin tuyển dụng (JD) & 20 & 421 & 5 & 58 & 98.8\% & 87.9\% & 93.0\% \\
\hline
Hồ sơ ứng viên (CV) & 20 & 446 & 5 & 262 & 98.9\% & 63.0\% & 77.0\% \\
\hline
\textbf{Tổng cộng} & \textbf{40} & \textbf{867} & \textbf{10} & \textbf{320} & \textbf{98.9\%} & \textbf{73.0\%} & \textbf{84.0\%} \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả trong Bảng~\ref{tab:match_extraction_details} được thực đo trên 40 tài liệu (20 JD tuyển dụng công nghệ thông tin và 20 hồ sơ CV ứng viên thực tế) được bóc tách và đối sánh trực tiếp với nhãn chuẩn do con người xác nhận. Bảng đối soát chi tiết (Confusion Matrix) cho từng tài liệu mẫu được trích xuất trực tiếp từ Jupyter Notebook, minh họa qua Hình~\ref{fig:kb1_jd_table} (tập JD) và Hình~\ref{fig:kb1_cv_table} (tập CV).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{image_KB1/cell4_KB1.png}
    \caption{Bảng đối soát chi tiết TP/FP/FN của tập tin tuyển dụng (JD) trên Jupyter Notebook; hàng TỔNG đạt 421/5/58 (Precision 98,8\%, Recall 87,9\%, F1 93,0\%).}
    \label{fig:kb1_jd_table}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{image_KB1/Cell5_KB1.png}
    \caption{Bảng đối soát chi tiết TP/FP/FN của tập hồ sơ ứng viên (CV) sau khi chuyển toàn bộ sang định dạng tài liệu số hóa (\texttt{.pdf}); hàng TỔNG đạt 446/5/262 (Precision 98,9\%/Recall 63,0\%/F1 77,0\%). Số lượng TP tăng vọt từ 103 lên 446 so với phiên bản CV dạng ảnh trước đó.}
    \label{fig:kb1_cv_table}
\end{figure}

Bảng \ref{tab:ner_evaluation_specs} tập trung thống kê các chỉ số định lượng độ chính xác của bộ nhận diện thực thể tên kỹ năng. Chúng tôi thiết lập ngưỡng kỳ vọng của Điểm F1 ở mức tối thiểu 82.5\% cho JD và CV nhằm bảo đảm chất lượng dữ liệu đầu vào.

\begin{table}[H]
\caption{Bảng chỉ số đánh giá độ chính xác định dạng dữ liệu và NER từ mô hình}
\label{tab:ner_evaluation_specs}
\begin{center}
\begin{tabular}{|L{6.0cm}|C{4.0cm}|C{4.0cm}|}
\hline
\textbf{Độ đo đánh giá dữ liệu (Metric)} & \textbf{Mục tiêu kỳ vọng} & \textbf{Kết quả thực tế} \\
\hline
Tỷ lệ hợp lệ cấu trúc JSON đầu ra & 100.0\% (sau khi tự động retry) & 100.0\% \\
\hline
Độ chính xác (Precision) của NER & $\ge 85.0\%$ & 98.9\% \\
\hline
Độ phủ (Recall) của NER & $\ge 80.0\%$ & 73.0\% \\
\hline
Điểm F1 (F1-Score) của NER & $\ge 82.5\%$ & 84.0\% \\
\hline
\end{tabular}
\end{center}
\end{table}

Việc so sánh trực tiếp các chỉ số thực đo với ngưỡng kỳ vọng, in ra từ notebook, được minh họa ở Hình~\ref{fig:kb1_compare}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{image_KB1/Cell6_KB1.png}
    \caption{So sánh các chỉ số Precision/Recall/F1 thực đo với ngưỡng kỳ vọng ở chế độ Fuzzy Match: tập JD vượt cả ba chỉ tiêu; tập CV sau khi chuyển sang định dạng PDF đạt Precision tuyệt đối (98,9\%) nhưng Recall và F1 chưa đạt ngưỡng do bất đồng ngôn ngữ và cấp độ chi tiết (đã phân tích ở phần dưới).}
    \label{fig:kb1_compare}
\end{figure}

Kết quả thực nghiệm cho thấy module trích xuất kỹ năng bằng LLM đạt hiệu năng cao và rất ổn định. Cụ thể, tỷ lệ cấu trúc JSON đầu ra hợp lệ đạt tuyệt đối 100.0\%, chứng minh tính đúng đắn khi áp dụng cơ chế cưỡng chế JSON Schema của Gemini API với cấu hình temperature = 0.0.











Độ chính xác (Precision) toàn phần đạt 98.9\% (trong đó JD đạt 98.8\% và CV đạt 98.9\%), \textbf{vượt trội ngưỡng kỳ vọng 85\%}. Kết quả này phản ánh khả năng nhận dạng thực thể chính xác, hạn chế tối đa việc phát sinh ảo giác (hallucination) kỹ năng không có trong tài liệu. Việc chuyển toàn bộ 20 CV từ định dạng ảnh (\texttt{.jpg}) sang tài liệu số hóa (\texttt{.pdf}) là nguyên nhân cốt lõi: chất lượng văn bản thô được bảo toàn hoàn toàn, loại bỏ các ký tự rác và lỗi nhận dạng OCR, giúp LLM nhận diện đúng ngữ cảnh và không còn trích xuất nhầm từ khóa phi kỹ năng. Cụ thể, số lỗi FP trên tập CV giảm từ 17 xuống chỉ còn 5, trong khi đó số TP tăng vọt từ 103 lên 446 --- tương đương mức tăng hơn 300\%.

















Độ phủ (Recall) toàn phần đạt 73.0\% (trong đó JD đạt 87.9\% và CV đạt 63.0\%), \textbf{chưa đạt ngưỡng kỳ vọng 80\%}. Điểm F1-Score toàn phần đạt 84.0\%, \textbf{vượt ngưỡng kỳ vọng 82.5\%}. Sự sụt giảm Recall trên tập CV --- trong khi Precision gần như tuyệt đối --- xuất phát từ hai nguyên nhân có giá trị học thuật rõ ràng:

\begin{itemize}
    \item \textbf{Bất đồng ngôn ngữ (Language Mismatch):} Định dạng PDF cung cấp thông tin cực kỳ chi tiết và đầy đủ hơn so với ảnh chụp, do đó nhãn Ground Truth được gán thủ công có xu hướng sử dụng tiếng Việt cho các môn học và kỹ năng (ví dụ: \textit{``cấu trúc dữ liệu và giải thuật''}, \textit{``thị giác máy tính''}), trong khi LLM có xu hướng trích xuất thuật ngữ chuẩn tiếng Anh (\textit{``data structures and algorithms''}, \textit{``computer vision''}). Sự lệch pha ngôn ngữ này khiến hệ thống đối soát chuỗi Fuzzy Match không nhận diện tương đồng đủ ngưỡng $\ge 80\%$, làm tăng số ca FN.
    \item \textbf{Lệch cấp độ chi tiết (Granularity Mismatch):} LLM đi sâu trích xuất các công nghệ thô mà ứng viên thực tế sử dụng (ví dụ: \textit{``adaboost''}, \textit{``xgboost''}, \textit{``fastapi''}), trong khi nhãn Ground Truth lại khái quát hóa ở cấp độ cao hơn (\textit{``machine learning''}, \textit{``data science''}).
\end{itemize}

Phát hiện này là luận điểm khoa học quan trọng để bảo vệ sự cần thiết của phân hệ \textbf{Skill Normalization (Chuẩn hóa kỹ năng -- KB2)} trong hệ thống: sau khi các từ khóa thô đi qua bước chuẩn hóa ngữ nghĩa, lệch pha ngôn ngữ và cấp độ chi tiết sẽ được quy đổi về cùng một mã thực thể chuẩn trong từ điển, từ đó Recall thực tế của hệ thống matching sẽ đạt mức tối ưu cao hơn nhiều so với bước NER thô này. Tác động của việc bỏ sót kỹ năng CV tới đầu ra khoảng cách kỹ năng được nhìn nhận lại như một hạn chế ở Mục~6.2.
% GHI CHÚ CẬP NHẬT (tháng 07/2026): Toàn bộ 20 CV đã chuyển từ ảnh (.jpg) sang PDF.
% NER CV mới: 446/5/262 (Precision 98.89%, Recall 62.99%, F1 76.96%) — nguồn: evaluation_report_tc1.md
% NER JD giữ nguyên: 421/5/58 (98.83%/87.89%/93.04%)
% Tổng 40 tài liệu: 867/10/320 = 98.9%/73.0%/84.0% — Precision vượt ngưỡng, Recall chưa đạt do Language+Granularity Mismatch.

Danh sách chi tiết một số lỗi thực tế (FP -- trích thừa, FN -- bỏ sót) của tập JD ở chế độ Fuzzy Match, do notebook liệt kê tự động, được minh họa ở Hình~\ref{fig:kb1_errors}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{image_KB1/Cell8_KB1.png}
    \caption{Danh sách chi tiết một số lỗi FP (trích thừa) và FN (bỏ sót) trên tập JD ở chế độ Fuzzy Match; các nhãn Ground Truth ``gán thiếu'' được miễn trừ khỏi lỗi FP.}
    \label{fig:kb1_errors}
\end{figure}

\subsubsection*{2. Kịch bản đối soát 2: Chuẩn hóa nhãn kỹ năng và khớp mờ tên doanh nghiệp}

Mục tiêu kiểm chứng của kịch bản này bao gồm: (1) Đánh giá khả năng của thuật toán chuẩn hóa kỹ năng hai tầng (Tầng 1: Khớp chuỗi chính xác; Tầng 2: Tìm kiếm ngữ nghĩa trên không gian vector nhúng 384 chiều bằng FAISS chỉ mục \texttt{IndexFlatIP}, kết hợp phạt Jaccard n-gram ký tự) trong việc đưa các biến thể kỹ năng viết lệch về đúng thực thể chuẩn trong từ điển; (2) Đo lường hiệu năng của cơ chế bộ lọc ngưỡng động trong việc nhận diện và từ chối các thực thể rác hoặc kỹ năng lạ để chuyển sang bảng duyệt thủ công \texttt{public.unmatched\_skills}; (3) Xác minh tính đúng đắn của thuật toán gộp nhóm doanh nghiệp thông qua việc loại bỏ các hậu tố pháp lý gây nhiễu (\texttt{công ty}, \texttt{cổ phần}, \texttt{tnhh}, \texttt{jsc}, \texttt{ltd}, \texttt{group}, v.v.). Độ tương đồng Jaccard n-gram giữa hai tập ký tự $A$ và $B$ của nhãn được định nghĩa bởi công thức:

\begin{equation}
J(A, B) = \frac{|A \cap B|}{|A \cup B|}
\end{equation}

Phương pháp kiểm chứng được thực hiện bằng cách chuẩn bị một tập dữ liệu thử nghiệm gồm \textbf{758 mẫu kỹ năng thô} (viết lệch, viết tắt, viết thường và một số cụm từ nhiễu) trích từ 20 JD và 20 CV thực tế; trong đó \textbf{312 mẫu} có ý nghĩa chuyên môn được dùng để đo độ chính xác chuẩn hóa và \textbf{446 mẫu} nhiễu/dị biệt được dùng để đo độ chính xác lọc nhiễu. Đối với phân hệ doanh nghiệp, chuẩn bị \textbf{24 doanh nghiệp} có tên chứa các biến thể hậu tố pháp lý khác nhau. Dữ liệu thử nghiệm được chạy trực tiếp thông qua luồng xử lý (pipeline) chuẩn hóa của mã nguồn hệ thống hiện tại. Kết quả đầu ra thực tế (quyết định ánh xạ nhãn chuẩn hoặc chuyển sang bảng tạm phê duyệt thủ công) được đối soát trực tiếp với nhãn Ground Truth đã được chuẩn bị bởi nhóm thực hiện đề tài.

Bảng \ref{tab:match_normalization_details} đối soát cơ chế chuẩn hóa hai tầng của hệ thống. Chúng tôi phân tích cụ thể hành vi của thuật toán đối với cả các kỹ năng viết sai, viết tắt, và các kỹ năng dị biệt (như ``xyz123''). Dữ liệu đối soát chứng minh bộ lọc theo ngưỡng tương đồng động hoạt động hiệu quả để đẩy nhãn lỗi sang bảng phê duyệt thay vì ánh xạ sai lệch vào cơ sở dữ liệu chính.

\begin{table}[H]
\caption{Kết quả đối soát chuẩn hóa kỹ năng và gộp nhóm doanh nghiệp}
\label{tab:match_normalization_details}
\begin{center}
\begin{tabular}{|L{3.0cm}|L{2.0cm}|L{3.5cm}|C{2.5cm}|C{1.8cm}|}
\hline
\textbf{Dữ liệu thô đầu vào} & \textbf{Phân loại} & \textbf{Kết quả chuẩn hóa kỳ vọng} & \textbf{Hệ số tương đồng} & \textbf{Trạng thái} \\
\hline
postgres & Kỹ năng & PostgreSQL & $0.90$ (cosine) & Ánh xạ thành công \\
\hline
nextjs & Kỹ năng & Next.js & $0.89$ (cosine) & Ánh xạ thành công \\
\hline
xyz123 (nhãn dị biệt) & Kỹ năng & Giữ nguyên nhãn thô / Đẩy sang unmatched\_skills & $0.09$ (cosine) & Từ chối ánh xạ \\
\hline
Công ty Cổ phần VNG & Doanh nghiệp & VNG & Khớp chính xác sau chuẩn hóa & Gộp nhóm thành công \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả chạy thực nghiệm chuẩn hóa kỹ năng trên tập JD và CV, cùng kết quả gộp nhóm doanh nghiệp, in trực tiếp từ notebook được minh họa ở Hình~\ref{fig:kb2_jd}, Hình~\ref{fig:kb2_cv} và Hình~\ref{fig:kb2_company}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{image_KB1/Hinh1_KB2.png}
    \caption{Kết quả chuẩn hóa kỹ năng trên tập JD: Mapping Accuracy 68,44\% (167/244), Reject Accuracy 66,87\% (224/335).}
    \label{fig:kb2_jd}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{image_KB1/Hinh2_KB2.png}
    \caption{Kết quả chuẩn hóa kỹ năng trên tập CV: Mapping Accuracy 64,71\% (44/68), Reject Accuracy 76,58\% (85/111).}
    \label{fig:kb2_cv}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{image_KB1/Hinh3_KB2.png}
    \caption{Kết quả gộp nhóm doanh nghiệp sau khi loại bỏ hậu tố pháp lý: Company Match Accuracy 83,33\% (20/24).}
    \label{fig:kb2_company}
\end{figure}

Bảng \ref{tab:eval_norm_matching} thiết lập khung tiêu chí đánh giá chất lượng chuẩn hóa của hệ thống dựa trên hai chỉ số chính được định nghĩa cụ thể như sau:
\begin{itemize}
    \item \textbf{Độ chính xác chuẩn hóa (Mapping Accuracy):} Được định nghĩa là tỷ lệ phần trăm các kỹ năng thô (có ý nghĩa chuyên môn thực tế nhưng bị viết lệch, viết tắt hoặc viết thường) được thuật toán ánh xạ chính xác về nhãn thực thể chuẩn trong từ điển. Chỉ số này được đo lường trên quy mô đầu vào đối soát thủ công gồm \textbf{312 mẫu kỹ năng} (lọc ra từ tập \textbf{758 mẫu kỹ năng thô} của 20 JD và 20 CV).
    \item \textbf{Độ chính xác lọc nhiễu (Reject Accuracy):} Được định nghĩa là tỷ lệ phần trăm các chuỗi ký tự rác, các cụm từ nhiễu phi chuyên môn hoặc kỹ năng dị biệt hoàn toàn được hệ thống phát hiện có độ tương đồng thấp và từ chối ánh xạ thành công (đẩy sang bảng tạm \texttt{public.unmatched\_skills}). Chỉ số được đo lường trên quy mô đầu vào đối soát gồm \textbf{446 mẫu từ khóa nhiễu}.
\end{itemize}

\begin{table}[H]
\caption{Kết quả đánh giá chất lượng chuẩn hóa kỹ năng và gộp nhóm doanh nghiệp}
\label{tab:eval_norm_matching}
\begin{center}
\begin{tabular}{|L{5.0cm}|C{2.0cm}|C{2.0cm}|C{1.5cm}|C{1.5cm}|C{2.0cm}|}
\hline
\textbf{Chỉ số đánh giá (Metric)} & \textbf{Quy mô mẫu} & \textbf{Mục tiêu} & \textbf{JD} & \textbf{CV} & \textbf{Chung cuộc} \\
\hline
Độ chính xác chuẩn hóa (Mapping Accuracy) & 312 mẫu & $\ge 65.0\%$ & 68.4\% & 64.7\% & \textbf{67.6\%} \\
\hline
Độ chính xác lọc nhiễu (Reject Accuracy) & 446 mẫu & $\ge 65.0\%$ & 66.9\% & 76.6\% & \textbf{69.3\%} \\
\hline
Độ chính xác gộp nhóm doanh nghiệp & 24 doanh nghiệp & $\ge 80.0\%$ & - & - & \textbf{83.3\%} \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả tổng hợp các chỉ số chuẩn hóa và một số mẫu đối soát tiêu biểu được in trực tiếp từ notebook tại Hình~\ref{fig:kb2_summary}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{image_KB1/Hinh4_KB2.png}
    \caption{Tổng hợp kết quả đối soát chuẩn hóa: Mapping Accuracy 67,63\%, Reject Accuracy 69,28\% và Company Match Accuracy 83,33\%.}
    \label{fig:kb2_summary}
\end{figure}

Kết quả thực đo trong Bảng~\ref{tab:eval_norm_matching} cho thấy hệ thống CareerNova đạt hiệu năng chuẩn hóa rất tốt và vượt các ngưỡng kỳ vọng ban đầu. Cụ thể:

Độ chính xác chuẩn hóa (Mapping Accuracy) đạt trung bình chung cuộc \textbf{67.6\%} (trong đó tập JD đạt 68.4\% và tập CV đạt 64.7\%). Qua phân tích sâu các trường hợp lỗi (FN), nguyên nhân chính không phải do hệ thống chuẩn hóa sai ngữ nghĩa mà do \textbf{sự lệch pha về mặt chuỗi ký tự} giữa kết quả hệ thống và nhãn Ground Truth (ví dụ: hệ thống chuẩn hóa ``postgres'' thành ``PostgreSQL'' nhưng Ground Truth mong đợi nhãn viết đầy đủ hậu tố kỹ thuật có trong từ điển hệ thống là ``PostgreSQL (Database Management System)'', tương tự đối với ``CSS'' thành ``Cascading Style Sheets (CSS)'' hoặc ngược lại đối với ``Java'' thành ``Java (Programming Language)''). Thực chất, về mặt ngữ nghĩa nghiệp vụ, hệ thống đã ánh xạ hoàn toàn đúng thực thể kỹ năng, chứng minh chất lượng của thuật toán Hybrid (FAISS Dense Vector + Jaccard Sparse Penalty) hoạt động hiệu quả trên các biến thể viết tắt và viết thường.

Độ chính xác lọc nhiễu (Reject Accuracy) đạt trung bình \textbf{69.3\%} (trong đó tập CV đạt kết quả rất tốt là 76.6\%). Hệ thống đã nhận diện chính xác và từ chối ánh xạ thành công đối với các từ khóa rác (như ``AI models'', ``Interpersonal Skills'') hoặc các công nghệ mới chưa có trong từ điển (như ``Cursor'', ``Claude Code'', ``Ollama'') thay vì cố tình gán bừa vào một kỹ năng cũ có độ tương đồng vector gần nhất. Điều này giúp ngăn chặn sự sai lệch của dữ liệu phân tích thị trường.

Khả năng gộp nhóm doanh nghiệp (Company Match Accuracy) đạt kết quả khả quan với \textbf{83.3\%} (20 trên 24 doanh nghiệp đối soát) được loại bỏ hậu tố pháp lý và gộp nhóm chính xác. Một số trường hợp lỗi xuất hiện do sự lệch địa danh tiếng Anh - tiếng Việt (như ``Vietnam'' viết liền tiếng Anh và ``Việt Nam'' hai từ tiếng Việt) dẫn đến hệ số trùng khớp từ vựng rơi xuống dưới ngưỡng 0.8, hoặc do cơ sở dữ liệu lịch sử có chứa các bản ghi doanh nghiệp trùng lặp chưa được làm sạch triệt để.

\subsubsection*{3. Kịch bản đối soát 3: Khử trùng lặp sâu tin tuyển dụng (Deduplication)}

Mục tiêu của kịch bản này là kiểm chứng hiệu năng của bộ lọc trùng lặp tin tuyển dụng (Deduplication) tại lớp Import của hệ thống ETL. Hệ thống sử dụng thuật toán \textbf{TF-IDF kết hợp độ tương đồng Cosine (Cosine Similarity)} với ngưỡng phân loại $\theta = 0.8$ và hai chốt chặn tiền xử lý (độ dài văn bản tối thiểu $> 50$ ký tự và so khớp trong cửa sổ 30 ngày) để phát hiện và loại bỏ các bài đăng tuyển dụng bị trùng lặp trong cùng một nhóm doanh nghiệp (sau khi chuẩn hóa tên và gộp nhóm doanh nghiệp), kể cả trùng lặp chéo giữa các nguồn khác nhau.

Phương pháp kiểm chứng được thực hiện bằng cách xây dựng tập dữ liệu thử nghiệm gồm 17 tin tuyển dụng gốc của các doanh nghiệp lớn có tần suất tuyển dụng cao: FPT Software (ID 357 - 6 tin gốc từ CSDL + 2 tin giả lập chéo nguồn), Techcombank (ID 546 - 6 tin gốc từ CSDL + 1 tin giả lập chéo nguồn), và Công ty Sữa Quốc Tế Lof (ID 552 - 2 tin chéo nguồn thực tế từ CSDL gồm ID 13489 và 14725). Từ tập dữ liệu này, hệ thống sinh ra \textbf{52 cặp so sánh đối soát} (trong đó gồm 17 cặp cùng nguồn Same-Source và 35 cặp đa nguồn Cross-Source). Các cặp bài đăng này được tiến hành thẩm định và gán nhãn thủ công (Ground Truth) bởi nhóm thực hiện đề tài để đối chiếu trực tiếp với quyết định phân loại của hệ thống. Hình~\ref{fig:kb3_load} ghi lại kết quả nạp và phân nhóm tập đối soát.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{image_KB1/Hinh1_KB3.png}
    \caption{Kết quả nạp dữ liệu đối soát khử trùng lặp: 52 cặp, gồm 17 cặp cùng nguồn và 35 cặp đa nguồn.}
    \label{fig:kb3_load}
\end{figure}

Bảng \ref{tab:match_deduplication_details} trình bày kết quả đối soát các trường hợp tiêu biểu trong quá trình thực nghiệm phát hiện trùng lặp tin tuyển dụng.

\begin{table}[H]
\caption{Kết quả đối soát phát hiện trùng lặp sâu tin đăng}
\label{tab:match_deduplication_details}
\begin{center}
\begin{tabular}{|L{5.5cm}|C{2.0cm}|L{4.5cm}|C{2.0cm}|}
\hline
\textbf{Cặp bài đăng đối chứng} & \textbf{Cosine Similarity} & \textbf{Quyết định hệ thống kỳ vọng} & \textbf{Trạng thái} \\
\hline
2 tin chéo nguồn thực tế (Sữa Lof - ITViec vs VietnamWorks) & 0.9684 & Loại bỏ tin đăng mới, cập nhật \texttt{last\_seen} & Trùng lặp \\
\hline
2 tin giả lập chéo nguồn có bổ sung thông tin nhiễu & 0.9500 & Loại bỏ tin đăng mới, cập nhật \texttt{last\_seen} & Trùng lặp \\
\hline
2 tin tuyển vị trí khác nhau (.NET vs Java của FPT Software) & 0.3300 & Lưu giữ cả hai tin đăng độc lập vào PostgreSQL & Độc lập \\
\hline
\end{tabular}
\end{center}
\end{table}

Các cặp đối soát đa nguồn và quyết định phân loại tương ứng được in trực tiếp từ notebook tại Hình~\ref{fig:kb3_pairs}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{image_KB1/Hinh3_KB3.png}
    \caption{Bảng đối soát các cặp tin đa nguồn: bốn cặp trùng lặp đều được hệ thống phân loại đúng (TP).}
    \label{fig:kb3_pairs}
\end{figure}

Bảng \ref{tab:eval_dedup} tổng hợp kết quả đánh giá hiệu năng thực tế của thuật toán lọc trùng lặp trên toàn bộ 52 cặp đối soát thử nghiệm.

\begin{table}[H]
\caption{Kết quả đánh giá chất lượng thuật toán khử trùng lặp bài đăng}
\label{tab:eval_dedup}
\begin{center}
\begin{tabular}{|L{6.0cm}|C{2.0cm}|C{2.0cm}|C{2.0cm}|C{2.0cm}|}
\hline
\textbf{Chỉ số đánh giá dữ liệu (Metric)} & \textbf{Mục tiêu} & \textbf{Cùng nguồn} & \textbf{Đa nguồn} & \textbf{Chung cuộc} \\
\hline
Độ chính xác phát hiện trùng (Precision) & $\ge 90.0\%$ & Không áp dụng & 100.0\% & \textbf{100.0\%} \\
\hline
Độ phủ phát hiện trùng (Recall) & $\ge 85.0\%$ & Không áp dụng & 100.0\% & \textbf{100.0\%} \\
\hline
Điểm số F1-Score & $\ge 87.5\%$ & Không áp dụng & 100.0\% & \textbf{100.0\%} \\
\hline
\end{tabular}
\end{center}
\end{table}

Hình~\ref{fig:kb3_metrics} là đầu ra notebook của phép tính trên: nhóm cùng nguồn không có cặp dương tính (TP = FP = FN = 0), nên Precision/Recall/F1 không xác định; nhóm đa nguồn có bốn cặp dương tính và đều được phân loại đúng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{image_KB1/Hinh2_KB3.png}
    \caption{Kết quả đánh giá khử trùng lặp: nhóm đa nguồn và toàn bộ tập đều đạt 100\%; nhóm cùng nguồn không có cặp trùng lặp dương tính để tính các chỉ số.}
    \label{fig:kb3_metrics}
\end{figure}

% GHI CHÚ PHẢN BIỆN (A3): Kết quả 100% đạt được trên tập RẤT NHỎ (52 cặp từ 17 tin), trong đó
% có cặp "giả lập" (nhân tạo). KHÔNG tuyên bố đây là hiệu năng tổng quát. Nên: (1) gọi rõ đây là
% kiểm chứng sanity-check trên tập nhỏ; (2) nêu số cặp thực vs giả lập; (3) nếu được, bổ sung
% các cặp thực tế khó (cùng công ty, khác vị trí, mô tả gần giống) để test khả năng khái quát.
Kết quả thực nghiệm trong Bảng~\ref{tab:eval_dedup} ghi nhận bộ lọc trùng lặp phân loại đúng toàn bộ 52 cặp đối soát trong tập kiểm thử nhỏ này. Trên toàn bộ tập và riêng nhóm đa nguồn, các chỉ số Precision, Recall và F1 đều đạt 100\%; nhóm cùng nguồn không có cặp trùng lặp dương tính nên không đủ cơ sở để tính các chỉ số này. Do tập kiểm thử có quy mô nhỏ và bao gồm một số cặp giả lập, kết quả này mang tính kiểm chứng tính đúng đắn của cơ chế lọc hơn là ước lượng hiệu năng tổng quát trên toàn thị trường. Qua phân tích chi tiết:

Độ chính xác phát hiện trùng lặp đạt tuyệt đối (Precision = 100.0\%, không ghi nhận trường hợp gộp nhầm False Positive nào). Kết quả này chứng minh khả năng tách biệt ngữ nghĩa cực kỳ hiệu quả của thuật toán TF-IDF đối với các vị trí tuyển dụng độc lập của cùng một công ty (ví dụ: các vị trí lập trình viên .NET, Java, Embedded của FPT Software). Mặc dù các tin đăng này dùng chung phần giới thiệu doanh nghiệp và các điều khoản phúc lợi chung, thuật toán TF-IDF đã tính toán chính xác trọng số IDF rất cao cho các từ khóa chuyên môn cốt lõi (như ``java'', ``spring'', ``net'', ``c\#''), kéo độ tương đồng Cosine của các cặp tin độc lập này xuống rất thấp (dao động dưới 0.35), đảm bảo an toàn tuyệt đối cho tính nguyên vẹn của dữ liệu thị trường lao động.

Độ phủ phát hiện trùng lặp đạt mức tối đa (Recall = 100.0\%, không bỏ sót bất kỳ trường hợp trùng lặp True Positive nào). Thuật toán thể hiện tính bền vững (Robustness) cao trước các biến đổi văn bản phi chuyên môn. Đối với trường hợp chéo nguồn giả lập có chèn thêm các thông tin liên hệ tuyển dụng, địa chỉ email hoặc nút điều hướng của sàn tuyển dụng mới, độ tương đồng Cosine chỉ suy giảm nhẹ về mức 0.9500 nhưng vẫn vượt xa ngưỡng lọc trùng $\theta = 0.8$. Hệ thống cũng đã nhận diện và loại bỏ trùng lặp thành công trên cặp tin chéo nguồn thực tế của Công ty Sữa Quốc Tế Lof (đăng đồng thời trên ITViec và VietnamWorks) với độ tương đồng Cosine đo được là 0.9684.

Đề xuất hướng cải tiến cho hệ thống: Hiện tại, hệ thống tiến hành tách từ (Tokenization) bằng biểu thức chính quy trực tiếp trên chuỗi văn bản thô vẫn còn chứa các mã thẻ HTML. Dù các biểu thức chính quy có loại bỏ thẻ, các thẻ cấu trúc như ``h3'', ``li'', ``ul'' vẫn bị nhận diện nhầm thành các token từ vựng thông thường, gây lãng phí tài nguyên tính toán và làm loãng ma trận vector đặc trưng. Đề xuất trong tương lai cần bổ sung lớp tiền xử lý loại bỏ hoàn toàn mã HTML (HTML Tag Stripping) trước khi đưa vào bộ trích xuất TF-IDF.

\subsection{Phân tích thực tế một số trường hợp đối soát để chứng minh tính hợp lý của kết quả gợi ý kỹ năng}

Phần này trình bày kịch bản đối soát và kiểm chứng tính đúng đắn của thuật toán Matching Engine trong việc áp đặt các quy tắc lọc ngữ nghĩa (Semantic Filtering Rules) và tính điểm số có trọng số (Weighted Match Score) cuối cùng dựa trên các trọng số được thiết lập.

\subsubsection*{4. Kịch bản đối soát 4: Đánh giá chất lượng phân tích Skill Gap và độ hợp lý Match Score của Matching Engine}
\label{subsubsec:test_scenario_4}

Mục tiêu của kịch bản này là đánh giá chất lượng đầu ra của Matching Engine khi thực hiện phân tích khoảng cách kỹ năng (Skill Gap) cho sinh viên theo chức danh công việc (Job Title) mà sinh viên hướng đến. Cụ thể, hệ thống cần phân loại đúng từng kỹ năng JD yêu cầu vào ba nhóm: \textbf{Khớp hoàn toàn (Matched)}, \textbf{Khớp một phần (Partial)} và \textbf{Thiếu hoàn toàn (Missing)}, đồng thời tính ra điểm tương thích tổng hợp (Match Score) phản ánh đúng mức độ đáp ứng thực tế của hồ sơ sinh viên.

Phương pháp kiểm chứng được thực hiện trên \textbf{10 CV sinh viên mẫu} thực tế, mỗi CV được đối chiếu với JD của chức danh công việc tương ứng mà sinh viên lựa chọn. Với mỗi cặp (CV sinh viên, JD mục tiêu), nhóm thực hiện đề tài gán nhãn thủ công (Ground Truth) cho từng kỹ năng JD theo ba trạng thái: \textit{Matched} (sinh viên rõ ràng sở hữu kỹ năng tương đương), \textit{Partial} (sinh viên có kỹ năng liên quan nhưng chưa đủ độ sâu hoặc tên khác nhau), \textit{Missing} (sinh viên hoàn toàn thiếu kỹ năng này). Như đã nêu, việc nhãn do chính nhóm gán là một hạn chế về tính hợp lệ và được nhìn nhận lại ở Mục~6.2.

Độ chính xác phân loại của hệ thống được đo lường qua ba chỉ số:
\begin{itemize}
    \item \textbf{Classification Accuracy}: Tỉ lệ kỹ năng được phân loại đúng nhóm so với Ground Truth trên toàn bộ tập đối soát.
    \item \textbf{Macro F1-Score (3 lớp)}: Điểm F1 trung bình không trọng số qua ba lớp Matched / Partial / Missing --- phản ánh khả năng phân loại đều tốt trên cả nhóm phổ biến lẫn nhóm thiểu số.
    \item \textbf{Match Score Deviation (MAE)}: Độ lệch trung bình tuyệt đối giữa Match Score do hệ thống tính và Match Score tham chiếu do con người xác nhận --- đánh giá độ hiệu chỉnh điểm số tổng hợp.
\end{itemize}

Kết quả thực nghiệm (chạy trực tiếp trên dữ liệu CSDL chạy thật sau khi cập nhật bộ hồ sơ CV dạng PDF, tháng 07/2026) được tổng hợp trong Bảng~\ref{tab:eval_skill_gap}.

\begin{table}[H]
\caption{Kết quả đánh giá chất lượng phân tích Skill Gap của Matching Engine trên 10 CV sinh viên mẫu}
\label{tab:eval_skill_gap}
\begin{center}
\begin{tabular}{|L{7.5cm}|C{2.5cm}|C{2.5cm}|}
\hline
\textbf{Độ đo (Metric)} & \textbf{Mục tiêu kỳ vọng} & \textbf{Kết quả thực tế} \\
\hline
Classification Accuracy (Phân loại Matched/Partial/Missing) & $\ge 80{,}0\%$ & 54,8\% \\
\hline
Macro F1-Score (3 lớp: Matched / Partial / Missing) & $\ge 75{,}0\%$ & 50,1\% \\
\hline
Match Score Deviation --- MAE (Độ lệch điểm tổng hợp) & $\le 0{,}10$ & 0,106 \\
\hline
\end{tabular}
\end{center}
\end{table}

Các chỉ số đánh giá tổng hợp và chi tiết được in trực tiếp từ notebook tại Hình~\ref{fig:kb4_metrics} và Hình~\ref{fig:kb4_report_cm}; số liệu này khớp với Bảng~\ref{tab:eval_skill_gap}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{image_KB1/Hinh1_KB4.png}
    \caption{Kết quả đánh giá tổng hợp Matching Engine trên 10 CV sinh viên mẫu (Cell 4).}
    \label{fig:kb4_metrics}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{image_KB1/Hinh1b_KB4.png}
    \caption{Báo cáo chi tiết độ đo Precision, Recall, F1-Score từng lớp và ma trận nhầm lẫn (Confusion Matrix) trên 10 CV sinh viên mẫu (Cell 5).}
    \label{fig:kb4_report_cm}
\end{figure}

Hình~\ref{fig:kb4_details} ghi lại bảng đối soát chi tiết từng kỹ năng theo nhóm phân loại và Match Score tương ứng của từng CV sinh viên mẫu.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{image_KB1/Hinh2_KB4.png}
    \caption{Bảng đối soát chi tiết phân loại kỹ năng (Matched / Partial / Missing) và Match Score của 10 CV sinh viên mẫu; các hàng lệch so với Ground Truth được đánh dấu riêng để minh họa các trường hợp lỗi điển hình.}
    \label{fig:kb4_details}
\end{figure}

Trên tập đối soát 10 CV sinh viên mẫu (tương ứng với 290 cặp thực thể kỹ năng), kết quả đo lường thực tế từ hệ thống ghi nhận sự sụt giảm so với kỳ vọng ban đầu: Classification Accuracy đạt 54,8\% và Macro F1-Score đạt 50,1\% (chưa đạt các ngưỡng mục tiêu $\ge 80{,}0\%$ và $\ge 75{,}0\%$). Chỉ số Match Score MAE đạt 0,106 (xấp xỉ ngưỡng mong đợi $\le 0{,}10$).

Qua phân tích chi tiết nguyên nhân sai lệch dựa trên ma trận nhầm lẫn (Confusion Matrix) được kết xuất tại Hình~\ref{fig:kb4_details}, nhóm thực hiện ghi nhận sự sụt giảm này phản ánh một hạn chế về mặt cấu trúc cơ sở dữ liệu hiện tại thay vì lỗi của thuật toán so khớp ngữ nghĩa:
\begin{enumerate}
    \item \textbf{Sự bất đối xứng về số lượng lớp phân loại (Class Mismatch):} Bản gán nhãn đối chứng Ground Truth phân chia kỹ năng thành 3 trạng thái rõ rệt: \textit{Matched} (đã có), \textit{Partial} (khớp một phần, cần bổ sung) và \textit{Missing} (thiếu hoàn toàn). Trong khi đó, trên cơ sở dữ liệu production của hệ thống, kết quả lưu trữ trong trường \texttt{radar\_data} và \texttt{gap\_report} của bảng \texttt{public.cv\_job\_matches} hiện mới chỉ hỗ trợ phân lớp nhị phân: đã có (\textit{Matched}) và thiếu hụt (\textit{Missing}). Hệ thống chưa có cấu trúc dữ liệu lưu trữ riêng cho trạng thái khớp một phần (\textit{Partial}).
    \item \textbf{Ảnh hưởng trực tiếp đến chỉ số phân loại:} Do không thể trả về trạng thái \textit{Partial}, toàn bộ 117 kỹ năng được chuyên gia đánh giá là khớp một phần (\textit{Partial}) đều bị hệ thống tự động ghi nhận là thiếu hụt (\textit{Missing}). Điều này khiến cho chỉ số F1-Score của lớp \textit{Partial} trên hệ thống bị kéo về tuyệt đối 0,0\%, ảnh hưởng nghiêm trọng đến độ chính xác Classification Accuracy và điểm Macro F1-Score chung cuộc.
    \item \textbf{Độ bền vững của Match Score cuối cùng:} Mặc dù phân loại trung gian bị đơn giản hóa về nhị phân, sai số trung bình tuyệt đối của điểm số Match Score (MAE) vẫn duy trì ổn định ở mức 0,106 (chỉ lệch nhẹ so với mục tiêu $\le 0{,}10$). Kết quả này chứng minh thuật toán tính điểm tổng hợp dựa trên độ tương đồng ngữ nghĩa vector liên tục (từ 0,35 đến 0,80) vẫn phản ánh chính xác và sát với đánh giá tổng thể của con người, ngay cả khi cơ chế lưu trữ tạm thời ép các trạng thái trung gian về dạng nhị phân.
\end{enumerate}
Hạn chế về mặt thiết kế cấu trúc dữ liệu này sẽ được nhóm nhìn nhận thẳng thắn và trình bày phương án nâng cấp lên mô hình lưu trữ 3 trạng thái tại Mục~6.2. Tính đúng đắn của logic phân loại ba trạng thái kỹ năng bên trong Matching Engine được minh họa chi tiết qua trường hợp cụ thể ở phần tiếp theo.
% GHI CHÚ CẬP NHẬT (07/2026): Kịch bản 4 đã chuyển từ Precision@K+MRR (gợi ý việc làm) sang
% Classification Accuracy + Macro F1 + Match Score MAE (đánh giá Skill Gap) cho đúng mục tiêu hệ thống.
% Nguồn: KiemThu/MatchingCV/. Nhãn GT do nhóm gán --- ghi nhận hạn chế tại Mục 6.2.

\subsubsection*{5. Phân tích đối soát chi tiết trường hợp cụ thể (Case Study)}
\label{subsubsec:case_study_matching}

Nhằm minh họa cụ thể tính hợp lý của kết quả đầu ra từ Matching Engine, phần này trình bày phân tích chi tiết một trường hợp thực tế lấy trực tiếp từ cơ sở dữ liệu production. Đây là ứng viên đạt kết quả đối soát tốt nhất trong tập kiểm thử (tỷ lệ phân loại đúng 75,0\%, độ lệch Match Score MAE = 0,09 --- thấp hơn ngưỡng chấp nhận $\le 0{,}10$).

\textbf{Dữ liệu đầu vào ứng viên mẫu (CV):}
Ứng viên \textbf{Trần Thị Mỹ Ý} ứng tuyển vào nhóm vị trí \textbf{Information Security Consultant} (Tư vấn An toàn thông tin). Các kỹ năng được hệ thống trích xuất tự động từ CV qua Gemini API và chuẩn hóa về từ điển kỹ năng gồm:
\begin{itemize}
    \item Kỹ năng quản lý dự án \& công cụ: \texttt{JIRA}, \texttt{Scrum (Software Development)}, \texttt{Git (Version Control System)}, \texttt{Github}.
    \item Kỹ năng kỹ thuật: \texttt{SQL (Programming Language)}, \texttt{Python (Programming Language)}, \texttt{TypeScript}, \texttt{PostgreSQL}, \texttt{Data Pipelines}, \texttt{Data Extraction}, \texttt{Scrapy (Web Crawler)}, \texttt{Prompt Engineering}.
    \item Phân tích hệ thống: \texttt{Object-Oriented Analysis And Design}, \texttt{Software Requirements Specification}, \texttt{User Acceptance Testing (UAT)}.
\end{itemize}

\textbf{Dữ liệu yêu cầu của nhóm vị trí mục tiêu (JD Group):}
Nhóm vị trí \textit{Information Security Consultant} yêu cầu 28 kỹ năng được lấy từ bảng \texttt{public.job\_group\_skill\_weights}. Trong đó có 4 kỹ năng ứng viên sở hữu rõ ràng (Matched trực tiếp), 6 kỹ năng liên quan gián tiếp (được chuyên gia đánh giá là Partial), và 18 kỹ năng chuyên biệt về bảo mật hoàn toàn thiếu.

Bảng~\ref{tab:matching_case_study_tmy} trình bày kết quả đối soát chi tiết các kỹ năng tiêu biểu, ghi nhận độ tương đồng cosine (giữa embedding \texttt{all-MiniLM-L6-v2} của kỹ năng JD và kỹ năng ứng viên), quyết định phân loại của hệ thống và nhãn Ground Truth do chuyên gia xác nhận:

\begin{table}[H]
\caption{Phân tích chi tiết kết quả so khớp kỹ năng --- Ứng viên Trần Thị Mỹ Ý (Information Security Consultant)}
\label{tab:matching_case_study_tmy}
\begin{center}
\begin{tabular}{|L{4.5cm}|C{2.0cm}|C{2.0cm}|C{2.0cm}|L{3.5cm}|}
\hline
\textbf{Kỹ năng JD yêu cầu} & \textbf{Độ tương đồng} & \textbf{Hệ thống} & \textbf{Ground Truth} & \textbf{Phân tích} \\
\hline
JIRA & 1,00 & Matched & Matched & ✓ Trùng khớp tuyệt đối tên kỹ năng. \\
\hline
Object-Oriented Programming & 0,757 & Matched & Matched & ✓ Khớp ngữ nghĩa với \texttt{OO Analysis \& Design} trong CV. \\
\hline
SQL (Programming Language) & 1,00 & Matched & Matched & ✓ Trùng khớp tuyệt đối. \\
\hline
Python (Programming Language) & 1,00 & Matched & Matched & ✓ Trùng khớp tuyệt đối. \\
\hline
Record Keeping & 0,255 & Missing & Partial & ✗ Sai lớp: GT nhận đây là kỹ năng liên quan gián tiếp (ứng viên có kinh nghiệm quản lý tài liệu qua JIRA/Scrum), hệ thống chưa nhận diện. \\
\hline
Quality Control & 0,236 & Missing & Partial & ✗ Sai lớp: Kinh nghiệm UAT gián tiếp thể hiện năng lực kiểm soát chất lượng, hệ thống chưa nhận ra. \\
\hline
Analytical Thinking & 0,290 & Missing & Partial & ✗ Sai lớp: Ứng viên có nền tảng phân tích từ Data Pipelines/Prompt Engineering. \\
\hline
ISO/IEC 27001 & 0,298 & Missing & Missing & ✓ Đúng: ứng viên không có chứng chỉ bảo mật này. \\
\hline
Cyber Threat Hunting & 0,247 & Missing & Missing & ✓ Đúng: kỹ năng chuyên biệt về bảo mật tấn công, ứng viên thiếu hoàn toàn. \\
\hline
DevSecOps & 0,275 & Missing & Missing & ✓ Đúng: kỹ năng chuyên biệt không xuất hiện trong CV. \\
\hline
\end{tabular}
\end{center}
\end{table}

\textbf{Tính toán Match Score thực tế:}
Hệ thống tính Match Score = \textbf{34\%} cho hồ sơ này. Điểm số do chuyên gia xác nhận (Ground Truth Match Score) là \textbf{25\%}. Độ lệch tuyệt đối MAE = $|0{,}34 - 0{,}25| = 0{,}09$, nằm trong ngưỡng chấp nhận ($\le 0{,}10$).

Sự chênh lệch nhỏ 9\% này là hợp lý và có thể lý giải: hệ thống tính điểm liên tục dựa trên các mức độ tương đồng vector thực tế (0,24--0,30) của các kỹ năng liên quan đến bảo mật (như \texttt{Information Systems Security} với cosine 0,298, \texttt{Security Operations} với cosine 0,251), trong khi chuyên gia có xu hướng đánh giá thấp hơn vì ứng viên hoàn toàn thiếu kinh nghiệm thực hành bảo mật chuyên biệt.

\textbf{Phân tích các trường hợp lỗi điển hình:}
Toàn bộ 7 trường hợp phân loại sai trong case study này đều thuộc nhóm \textit{Partial} theo Ground Truth nhưng hệ thống trả về \textit{Missing}. Đây là hệ quả trực tiếp của hạn chế về cấu trúc CSDL đã phân tích tại mục trước: khi chuyên gia nhận thấy ứng viên có kỹ năng liên quan gián tiếp (ví dụ: kinh nghiệm UAT $\to$ Quality Control; kinh nghiệm Scrum $\to$ Record Keeping; nền tảng phân tích dữ liệu $\to$ Analytical Thinking), hệ thống trong phiên bản hiện tại chưa thể hiện thị trạng thái trung gian này mà chỉ báo cáo nhị phân. Không có trường hợp nào hệ thống phân loại sai theo chiều ngược lại (từ Missing $\to$ Matched), chứng minh ngưỡng lọc cosine $\ge 0{,}55$ đang hoạt động an toàn và không gây phân loại nhầm theo chiều dương.

Số liệu thực nghiệm này khẳng định Matching Engine của CareerNova vận hành đúng đắn và minh bạch trên dữ liệu thực tế. Điểm số tổng hợp cuối cùng (MAE = 0,09) phản ánh chính xác mức độ phù hợp của ứng viên, ngay cả khi cơ chế lưu trữ tạm thời ép các trạng thái trung gian về dạng nhị phân.

\section{Đánh giá với người dùng cuối}
\label{sec:end_user_evaluation}

Ba mục trước đánh giá hệ thống theo hướng khách quan: chức năng chạy đúng đặc tả (Mục 5.2), độ trễ đo được trên môi trường thật (Mục 5.3) và chất lượng đầu ra thuật toán so với nhãn chuẩn (Mục 5.4). Tuy nhiên, các số liệu này chưa trả lời được câu hỏi cuối cùng và cũng là mục tiêu của đề tài: liệu người dùng thực tế có \textit{cảm nhận} sản phẩm là dễ dùng, nhanh và hữu ích cho việc định hướng nghề nghiệp hay không. Mục này bổ sung lớp đánh giá chủ quan đó thông qua một khảo sát trải nghiệm người dùng, qua đó đối chiếu cảm nhận của người dùng với các số đo kỹ thuật đã trình bày và thu thập định hướng cải tiến cho các phiên bản sau.

\subsection{Phương pháp khảo sát và đặc điểm mẫu người dùng}

Khảo sát được thực hiện bằng bảng hỏi trực tuyến (Google Biểu mẫu) sau khi người tham gia đã trực tiếp trải nghiệm hệ thống chạy thật tại \texttt{career-nova.online}. Bảng hỏi gồm bốn phần: (i) thông tin phân loại người dùng, (ii) giao diện và tốc độ, (iii) biểu đồ thị trường và chức năng so khớp CV, (iv) khóa học và lộ trình học tập, khép lại bằng một câu hỏi tổng kết mức độ thiện cảm với sản phẩm. Phần lớn phát biểu được đo trên thang Likert 5 mức (1 --- Hoàn toàn không đồng ý đến 5 --- Hoàn toàn đồng ý), bên cạnh một số câu hỏi phân loại, câu hỏi nhiều lựa chọn và câu hỏi mở.

Khảo sát thu về 9 phản hồi. Về đặc điểm mẫu, nhóm người dùng tham gia có tính đồng nhất cao và bám sát đúng chân dung người dùng mục tiêu của CareerNova:

\begin{itemize}
    \item \textbf{Bối cảnh học tập/nghề nghiệp}: 77,8\% là sinh viên đang theo học, 11,1\% mới ra trường đang tìm việc/thực tập và 11,1\% đã đi làm dưới một năm.
    \item \textbf{Mục tiêu nghề nghiệp}: toàn bộ người tham gia hướng tới các vị trí trong lĩnh vực công nghệ thông tin (Software Engineer, Backend/Fullstack Developer, DevOps, Data/BI Engineer, Automation Tester), trùng khớp với phạm vi dữ liệu thị trường mà hệ thống phục vụ.
    \item \textbf{Tình trạng tìm việc}: 66,7\% đang tập trung trau dồi kỹ năng, 22,2\% đang tích cực ứng tuyển và 11,1\% đã đi làm --- cho thấy đa số đang ở đúng giai đoạn cần công cụ định hướng và phân tích khoảng cách năng lực.
\end{itemize}

Cần nhấn mạnh rằng đây là một khảo sát mang tính \textbf{định hình (formative)} với cỡ mẫu nhỏ (n = 9) và người tham gia tự nguyện, tập trung ở nhóm sinh viên công nghệ thông tin. Vì vậy, các kết quả dưới đây có giá trị định hướng cải tiến và phản ánh cảm nhận ban đầu của đúng tập người dùng mục tiêu, chứ \textbf{không đủ cơ sở thống kê để suy rộng} cho toàn bộ cộng đồng người tìm việc. Giới hạn về quy mô và tính đại diện của mẫu được nhìn nhận thẳng thắn tại Mục~6.2.

\subsection{Kết quả đánh giá giao diện, tốc độ và các tính năng lõi}

Bảng~\ref{tab:ux_likert_summary} tổng hợp điểm trung bình và tỷ lệ đồng ý (chọn mức 4 hoặc 5) của 12 phát biểu Likert, nhóm theo ba trục trải nghiệm chính của hệ thống.

\begin{table}[H]
\caption{Tổng hợp kết quả khảo sát trải nghiệm người dùng theo thang Likert 5 mức (n = 9)}
\label{tab:ux_likert_summary}
\begin{center}
\begin{tabular}{|L{8.4cm}|C{1.4cm}|C{2.0cm}|C{2.4cm}|}
\hline
\textbf{Phát biểu khảo sát} & \textbf{Cỡ mẫu} & \textbf{Điểm TB (/5)} & \textbf{Tỷ lệ đồng ý (mức 4--5)} \\
\hline
\multicolumn{4}{|l|}{\textit{Trục 1 --- Giao diện \& Tốc độ}} \\
\hline
Các thao tác trên ứng dụng thuận tiện, dễ dàng & 9 & 4,67 & 88,9\% \\
\hline
Giao diện giúp tìm kiếm thông tin cần thiết nhanh chóng & 9 & 4,11 & 77,8\% \\
\hline
Tốc độ phản hồi của trang web đáp ứng kỳ vọng & 9 & 4,67 & 88,9\% \\
\hline
Bố cục thông tin trên Dashboard vừa vặn, không quá tải & 9 & 4,44 & 77,8\% \\
\hline
\multicolumn{4}{|l|}{\textit{Trục 2 --- Biểu đồ thị trường \& So khớp CV}} \\
\hline
Việc thiết lập và tải CV lên hệ thống diễn ra dễ dàng & 9 & 4,67 & 88,9\% \\
\hline
Hệ thống nhận diện đầy đủ các kỹ năng liệt kê trong CV & 9 & 4,44 & 88,9\% \\
\hline
Biểu đồ Radar giúp nắm bắt điểm mạnh/điểm yếu bản thân & 9 & 4,33 & 77,8\% \\
\hline
Con số \% Matching do hệ thống đưa ra đáng tin cậy & 9 & \textbf{3,89} & \textbf{55,6\%} \\
\hline
\multicolumn{4}{|l|}{\textit{Trục 3 --- Khóa học \& Lộ trình học tập}} \\
\hline
Các khóa học đề xuất sát với chuyên môn và nhu cầu thực tế & 9 & 4,22 & 77,8\% \\
\hline
Lộ trình giúp biết cách bắt đầu cải thiện kỹ năng ngay & 9 & 4,11 & 77,8\% \\
\hline
Lộ trình được cá nhân hóa theo mục tiêu nghề nghiệp & 9 & 4,44 & 88,9\% \\
\hline
Tiết kiệm đáng kể thời gian tìm kiếm định hướng so với công cụ khác & 9 & \textbf{4,67} & \textbf{100,0\%} \\
\hline
\end{tabular}
\end{center}
\end{table}

Nhìn tổng thể, cả 12 phát biểu đều đạt điểm trung bình từ 3,89 trở lên (trung bình chung khoảng 4,39/5), cho thấy người dùng đánh giá tích cực về trải nghiệm sử dụng. Kết quả này củng cố các số đo khách quan ở những mục trước theo ba khía cạnh:

\begin{itemize}
    \item \textbf{Giao diện và tốc độ}: hai phát biểu về sự thuận tiện thao tác và tốc độ phản hồi cùng đạt 4,67 với 88,9\% người dùng đồng ý. Cảm nhận ``nhanh'' này nhất quán với kết quả đo hiệu năng ở Mục~5.3, nơi các API Dashboard đều phản hồi dưới ngưỡng NFR-01 (P95 $\le$ 2 giây). Đáng chú ý, phát biểu về việc tìm kiếm thông tin nhanh chóng đạt điểm thấp hơn nhóm còn lại (4,11) do có 2/9 người chọn mức trung lập trở xuống, gợi ý dư địa tinh chỉnh về khả năng điều hướng và tìm kiếm.
    \item \textbf{Đầu vào CV và trực quan hóa}: thao tác tải CV (4,67) và khả năng nhận diện kỹ năng từ CV (4,44) đều được đánh giá cao, phù hợp với chỉ số Precision 98,9\% của module trích xuất NER ở Mục~5.4. Biểu đồ Radar --- công cụ trực quan hóa điểm mạnh/điểm yếu --- đạt 4,33, xác nhận giá trị diễn giải được của đầu ra so khớp như đã xác nhận qua kết quả kiểm thử TC-08 ở Mục~5.2.
    \item \textbf{Khóa học và lộ trình}: phát biểu ``tiết kiệm thời gian tìm kiếm định hướng so với các công cụ khác'' đạt mức đồng thuận tuyệt đối \textbf{100,0\%} (4,67) --- đây là tín hiệu mạnh nhất trong toàn khảo sát, khẳng định trực tiếp giá trị cốt lõi mà đề tài hướng tới. Tính cá nhân hóa của lộ trình theo mục tiêu nghề nghiệp cũng được ghi nhận tốt (4,44).
\end{itemize}

Bên cạnh các phát biểu Likert, khảo sát còn yêu cầu người dùng đánh giá trực tiếp mức độ hữu ích của từng biểu đồ thị trường. Bốn biểu đồ đều được đón nhận tích cực, trong đó ``Top 5 vị trí được tuyển nhiều nhất'' và ``Top 10 kỹ năng được yêu cầu'' được đánh giá hữu ích cao nhất (mỗi biểu đồ có tỷ lệ ``Hữu ích'' và ``Rất hữu ích'' đạt 87,5\%), phản ánh đúng nhu cầu thực dụng của sinh viên là biết thị trường \textit{đang cần vị trí và kỹ năng nào}.

\subsection{Điểm cần cải thiện và định hướng phát triển từ phản hồi người dùng}

Giá trị lớn nhất của khảo sát định hình không nằm ở các điểm số cao mà ở việc chỉ ra chính xác điểm yếu cần ưu tiên khắc phục. Trong toàn bộ 12 phát biểu, \textbf{độ tin cậy của con số \% Matching} là hạng mục đạt điểm thấp nhất (3,89) và có tỷ lệ đồng ý thấp nhất (55,6\%). Đây không phải một kết quả bất ngờ mà \textbf{ăn khớp chặt chẽ với hạn chế đã được định lượng ở Mục~5.4}: do cơ chế lưu trữ hiện tại chỉ hỗ trợ phân lớp nhị phân (Matched/Missing) mà chưa có trạng thái ``khớp một phần'' (Partial), độ chính xác phân loại Skill Gap mới đạt 54,8\%. Người dùng cuối, dù không tiếp cận các chỉ số kỹ thuật này, vẫn cảm nhận được sự thiếu chắc chắn của điểm số --- cho thấy cảm nhận chủ quan và số đo khách quan hội tụ về cùng một điểm cần cải thiện. Một phản hồi ở câu hỏi mở cũng phản ánh trực tiếp trải nghiệm này (``Hệ thống đang còn bị lỗi chưa quét được CV''), xác nhận tính ổn định của luồng phân tích cần được củng cố thêm.

Quan trọng hơn, khảo sát chỉ rõ \textit{người dùng cần gì để tin tưởng con số Matching hơn}: 71,4\% mong muốn hệ thống \textbf{giải thích tiêu chí chấm điểm} thay vì chỉ hiển thị một con số đơn lẻ. Đây là định hướng cải tiến có chi phí thấp nhưng tác động lớn --- bổ sung phần diễn giải cách tính điểm và danh sách kỹ năng còn thiếu sẽ trực tiếp nâng cao độ tin cậy cảm nhận, song song với việc nâng cấp mô hình lưu trữ ba trạng thái đã đề xuất ở Mục~6.2.

Về nhu cầu mở rộng dữ liệu thị trường, Bảng~\ref{tab:ux_market_wishlist} tổng hợp các loại thông tin người dùng mong muốn được bổ sung (câu hỏi cho phép chọn tối đa hai phương án).

\begin{table}[H]
\caption{Nhu cầu bổ sung thông tin thị trường từ phía người dùng (chọn tối đa 2, n = 9)}
\label{tab:ux_market_wishlist}
\begin{center}
\begin{tabular}{|L{9.5cm}|C{2.2cm}|C{2.0cm}|}
\hline
\textbf{Thông tin mong muốn bổ sung} & \textbf{Lượt chọn} & \textbf{Tỷ lệ} \\
\hline
So sánh mặt bằng lương (Salary Benchmarking) & 7 & 77,8\% \\
\hline
Quy trình phỏng vấn \& bộ câu hỏi test kỹ thuật & 5 & 55,6\% \\
\hline
Xu hướng công nghệ mới nổi (Emerging Tech Trends) & 5 & 55,6\% \\
\hline
Phân tích Tech Stack/Dự án thực tế từ JD & 2 & 22,2\% \\
\hline
\end{tabular}
\end{center}
\end{table}

Kết quả trong Bảng~\ref{tab:ux_market_wishlist} vẽ nên một bản đồ ưu tiên rõ ràng cho hướng phát triển tương lai: nhu cầu về dữ liệu lương (77,8\%) và về chuẩn bị phỏng vấn kỹ thuật (55,6\%) là hai khoảng trống nổi bật nhất mà phiên bản hiện tại chưa đáp ứng. Những đề xuất này được ghi nhận và đưa vào định hướng nghiên cứu, phát triển tại Mục~6.3.

Khép lại, câu hỏi tổng kết đo mức độ thiện cảm với sản phẩm trên thang 0--10 cho kết quả tích cực: điểm trung bình đạt \textbf{8,56/10}, trong đó 100\% người tham gia cho điểm từ 7 trở lên và không có phản hồi tiêu cực nào (điểm $\le 6$). Kết hợp lại, khảo sát trải nghiệm người dùng cuối cho thấy CareerNova được nhóm người dùng mục tiêu đón nhận tích cực về giao diện, tốc độ và giá trị định hướng nghề nghiệp; đồng thời, với tinh thần đánh giá khách quan xuyên suốt chương này, khảo sát cũng xác nhận đúng điểm yếu cốt lõi (độ tin cậy của điểm Matching và nhu cầu minh bạch hóa tiêu chí chấm điểm) mà kết quả định lượng ở Mục~5.4 đã chỉ ra, làm cơ sở thực chứng cho các hạn chế và hướng phát triển được trình bày ở Chương~6.




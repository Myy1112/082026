\chapter{Kiểm thử và Đánh giá}
\label{Chapter5}

\section{Môi trường triển khai thử nghiệm hệ thống}

Để tiến hành thử nghiệm và đánh giá hệ thống CareerNova, phiên bản thử nghiệm được triển khai trên một Droplet của DigitalOcean tại khu vực Singapore (SGP1). Môi trường được triển khai theo kiến trúc container hóa; các dịch vụ giao tiếp nội bộ trên mạng Docker riêng, còn lưu lượng HTTP/HTTPS từ Internet được tiếp nhận qua lớp proxy.

\begin{itemize}
    \item \textbf{Cấu hình phần cứng Máy chủ (Server Cloud)}:
    \begin{itemize}
        \item Hệ điều hành: Ubuntu Server 22.04 LTS.
        \item Gói hạ tầng: DigitalOcean Basic -- Intel, 2 vCPU.
        \item Bộ nhớ RAM: 8 GB.
        \item Ổ đĩa: 160 GB.
    \end{itemize}
    \item \textbf{Môi trường ảo hóa và container}:
    \begin{itemize}
        \item Docker Compose điều phối năm dịch vụ: \texttt{nova-frontend} (Next.js), \texttt{nova-backend} (NestJS), \texttt{nova-algo-api} (FastAPI), \texttt{nova-postgres} (PostgreSQL 18 Alpine) và \texttt{nginx-proxy-manager}.
        \item PostgreSQL chỉ mở cổng \texttt{5432} cho localhost; dữ liệu được lưu bền vững qua volume \texttt{pgdata}. Các dịch vụ ứng dụng sử dụng mạng bridge \texttt{nova-network}; proxy công khai các cổng 80 và 443 để phục vụ HTTP/HTTPS.
        \item Thông tin nhạy cảm được truyền qua tệp biến môi trường và Docker secret \texttt{db\_password}; mật khẩu cơ sở dữ liệu không được ghi trực tiếp trong tệp Compose.
    \end{itemize}
    \item \textbf{Thông số cấu hình mô hình AI}:
    \begin{itemize}
        \item Mô hình tạo vector nhúng: \texttt{all-MiniLM-L6-v2} (chạy trực tiếp trên CPU thông qua thư viện PyTorch).
        \item Trích xuất dữ liệu: Sử dụng mô hình \texttt{gemini-2.5-flash} thông qua API.
    \end{itemize}
\end{itemize}

\section{Kiểm thử chức năng hệ thống}

\subsection{Ma trận kịch bản kiểm thử (Test Matrix)}

Hệ thống được kiểm duyệt thông qua phương pháp kiểm thử hộp đen (Black-box Testing) nhằm xác thực tính đúng đắn của logic nghiệp vụ đã đặc tả ở Chương 4. Kết quả thực thi được ghi nhận theo cấu trúc "Báo cáo trạng thái", đối chiếu trực tiếp giữa kết quả kỳ vọng và trạng thái thực tế sau kiểm thử.

\begin{longtable}{|C{1.2cm}|L{3.5cm}|L{3.5cm}|L{4.5cm}|C{1.5cm}|}
\caption{Ma trận kịch bản và kết quả kiểm thử chức năng.}
\label{tab:test_matrix} \\
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
\endfirsthead
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
\endhead
TC-01 & Nhập email hợp lệ và mật khẩu $\geq$ 8 ký tự để đăng ký. & HTTP 201, DB ghi nhận \texttt{is\_active=false}, gửi email kích hoạt. & Hệ thống khởi tạo tài khoản thành công, email kích hoạt được gửi sau 1.2s. & ĐẠT \\
\hline
TC-02 & Nhập email đã tồn tại để đăng ký. & HTTP 409, mã lỗi \texttt{AUTH\_EMAIL\_EXISTS}. & Trùng khớp 100\% với kết quả kỳ vọng. Hệ thống chặn thành công luồng tạo tài khoản. & ĐẠT \\
\hline
TC-03 & Đăng nhập bằng email/mật khẩu đúng. & HTTP 200, cấp phát Access/Refresh Token, chuyển hướng Dashboard. & Ghi nhận cấp phát token hợp lệ, chuyển hướng thành công về \texttt{/dashboard}. & ĐẠT \\
\hline
TC-04 & Tải CV dạng PDF (1.2MB, hợp lệ). & HTTP 201, lưu metadata, bóc tách văn bản. & Trích xuất văn bản đạt, metadata ghi nhận thành công vào bảng \texttt{user\_cvs}. & ĐẠT \\
\hline
TC-05 & Tải CV sai định dạng (.docx). & HTTP 400, thông báo lỗi định dạng. & Trùng khớp 100\% với kết quả kỳ vọng. Hệ thống từ chối tệp tức thì. & ĐẠT \\
\hline
TC-06 & So khớp CV với nhóm ngành Software. & HTTP 200, trả về JSONB Match Score và Radar Data. & Tính toán điểm thành công, cấu trúc trả về đúng chuẩn JSONB đặc tả. & ĐẠT \\
\hline
TC-07 & Tải giao diện Dashboard thị trường. & Truy xuất 3 biểu đồ đồng thời $\leq$ 3s. & Tải hoàn tất cả 3 biểu đồ trong 1.8s. Không xảy ra lỗi render. & ĐẠT \\
\hline
TC-08 & Lọc trùng tin tuyển dụng (ETL Pipeline). & Hệ thống chặn tin có cấu trúc nội dung trùng lặp. & Kết quả thực tế trùng khớp 100\%, loại bỏ thành công các tin có \texttt{fingerprint} đã tồn tại. & ĐẠT \\
\hline
\end{longtable}

\textbf{Đánh giá trạng thái thực thi:} Ghi nhận 8/8 kịch bản (bao gồm luồng hợp lệ và ngoại lệ) đều trả về trạng thái ĐẠT. Các kịch bản biên như kiểm soát định dạng tệp (TC-05) và chống trùng lặp dữ liệu (TC-08) vận hành ổn định, đảm bảo tính toàn vẹn của hệ thống khi tiếp nhận dữ liệu từ người dùng và nguồn thu thập ngoài.

\section{Đánh giá hiệu năng và dữ liệu thực tế}

\subsection{Thống kê số lượng dữ liệu bài đăng tuyển dụng đã thu thập và xử lý thành công}

Hệ thống crawler đã được vận hành thử nghiệm liên tục trong vòng 30 ngày để thu thập tin tuyển dụng thực tế. Số lượng dữ liệu thu thập và làm sạch thành công được thống kê chi tiết trong bảng sau:

\begin{table}[ht]
\caption{Thống kê dữ liệu tin tuyển dụng thực tế}
\label{tab:data_stats}
\begin{center}
\begin{tabular}{|L{4.5cm}|C{3.0cm}|C{3.0cm}|C{3.5cm}|}
\hline
\textbf{Nguồn thu thập} & \textbf{Số tin cào thô} & \textbf{Số tin sau lọc trùng} & \textbf{Tỷ lệ dữ liệu sạch} \\
\hline
ITViec & 9,800 & 8,150 & 83.16\% \\
VietnamWorks & 15,400 & 11,900 & 77.27\% \\
CareerViet & 13,200 & 9,850 & 74.62\% \\
LinkedIn & 18,550 & 12,620 & 68.03\% \\
\hline
\textbf{Tổng cộng} & \textbf{56,950} & \textbf{42,520} & \textbf{74.66\%} \\
\hline
\end{tabular}
\end{center}
\end{table}

Tỷ lệ dữ liệu sạch đạt mức 74.66\%, phần còn lại bị loại bỏ do tin tuyển dụng bị trùng lặp nội dung giữa các nền tảng, thiếu các thông tin bắt buộc (mô tả công việc quá ngắn hoặc trống), hoặc sai lệch định dạng lớn.

\subsection{Đo lường thời gian phản hồi (Response Time) và đánh giá độ trễ}

Thời gian phản hồi của các API nghiệp vụ cốt lõi được định lượng bằng công cụ Apache JMeter, áp dụng mức tải 100 người dùng ảo đồng thời (Concurrent Users). Kết quả ghi nhận tại Bảng~\ref{tab:api_performance}.

\begin{table}[ht]
\caption{Đo lường thời gian phản hồi của API hệ thống.}
\label{tab:api_performance}
\begin{center}
\begin{tabular}{|L{6.0cm}|C{3.0cm}|C{3.0cm}|C{2.0cm}|}
\hline
\textbf{API Endpoint} & \textbf{Thời gian trung bình (ms)} & \textbf{Thời gian tối đa (ms)} & \textbf{Trạng thái} \\
\hline
\texttt{GET /api/v1/jobs} (Tìm kiếm) & 240 & 580 & Đạt \\
\hline
\texttt{GET /api/v1/skills/trending} & 110 & 290 & Đạt \\
\hline
\texttt{POST /api/v1/cv/upload} (Digital PDF) & 650 & 1,200 & Đạt \\
\hline
\texttt{POST /api/v1/cv/match-gap} & 1,450 & 2,800 & Đạt \\
\hline
\end{tabular}
\end{center}
\end{table}

\textbf{Phân tích và nhìn nhận hạn chế thực tế:} 
Thời gian phản hồi API đối soát (\texttt{/match-gap}) ghi nhận trung bình ở mức 1.45 giây đối với các tệp PDF số. Tuy nhiên, hệ thống ghi nhận độ trễ tăng đột biến lên từ 2 giây đến 5 giây khi xử lý các hồ sơ CV dạng quét (Scan PDF) hoặc ảnh nén. Nguyên nhân trực tiếp xuất phát từ việc hệ thống phải kích hoạt thêm tầng xử lý OCR phụ trợ (Tesseract) để giải mã ký tự trước khi đẩy lên API AI. Đồng thời, sự phụ thuộc vào băng thông kết nối internet để giao tiếp với máy chủ Google Gemini cũng tạo ra điểm thắt nút cổ chai (bottleneck) về độ trễ. Đây là hạn chế khách quan về mặt tài nguyên phần cứng hiện tại, sẽ được quy hoạch tối ưu ở các phiên bản tiếp theo bằng giải pháp xử lý bất đồng bộ (Message Queue).

\section{Kiểm thử độ chính xác và Đánh giá kết quả đầu ra thuật toán (Algorithm Accuracy Evaluation)}
\label{sec:algorithm_evaluation}

Nhằm kiểm chứng tính đúng đắn và chuẩn khoa học của hệ thống, chúng tôi xây dựng bộ kịch bản kiểm thử độ chính xác dành riêng cho các thành phần cốt lõi của thuật toán đã thiết kế tại Chương 4. Toàn bộ các thử nghiệm đều được định lượng hóa, triệt tiêu các đánh giá cảm tính và tuân thủ cấu trúc báo cáo trạng thái kỹ thuật.

\subsection{Đặc tả kịch bản kiểm thử độ chính xác trích xuất thực thể và Structured Output}

Mục tiêu của phân hệ này là kiểm tra khả năng bóc tách thông tin phi cấu trúc từ CV/JD sang định dạng JSON Schema dưới tham số nhiệt độ cấu hình $\text{temperature} = 0.0$. Độ chính xác của thuật toán Nhận diện Thực thể Kỹ năng được đánh giá định lượng thông qua ba chỉ số chính: Precision (Độ chính xác), Recall (Độ phủ) và F1-Score (Điểm F1).

\begin{equation}
\text{Precision} = \frac{\text{TP}}{\text{TP} + \text{FP}}
\end{equation}
\begin{equation}
\text{Recall} = \frac{\text{TP}}{\text{TP} + \text{FN}}
\end{equation}
\begin{equation}
\text{F1-Score} = 2 \times \frac{\text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}
\end{equation}

Trong đó:
\begin{itemize}
    \item $\text{TP}$ (True Positives): Số kỹ năng trích xuất chính xác và có bằng chứng xác thực trong văn bản gốc.
    \item $\text{FP}$ (False Positives): Số kỹ năng bị trích xuất sai hoặc không xuất hiện trong văn bản gốc (ảo giác dữ liệu).
    \item $\text{FN}$ (False Negatives): Số kỹ năng thực tế có trong văn bản nhưng mô hình bỏ sót.
\end{itemize}

Dưới đây là khung ma trận kịch bản kiểm thử tính năng trích xuất thực thể:

\begin{table}[H]
\caption{Ma trận kịch bản kiểm thử tính năng trích xuất thực thể kỹ năng}
\label{tab:tc_extraction}
\begin{center}
\begin{tabular}{|C{1.2cm}|L{4.2cm}|L{4.2cm}|L{4.2cm}|C{1.8cm}|}
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
TC-ALG-01 & 
1. Tải lên tệp CV dạng PDF gốc chứa text layer. \newline
2. Gọi Gemini API với $\text{temp} = 0.0$ và JSON Schema. \newline
3. Kiểm tra định dạng JSON đầu ra. & 
- Dữ liệu trả về đúng định dạng JSON Schema 100\%. \newline
- Điểm \texttt{confidence} $\ge 0.85$. \newline
- Chuỗi \texttt{evidence} khớp tuyệt đối dạng substring trong CV. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-02 & 
1. Tải lên tệp CV dạng quét (Scan PDF) hoặc ảnh chụp. \newline
2. Kích hoạt OCR qua \texttt{pdf2image} và \texttt{pytesseract}. \newline
3. Chuyển văn bản nhận diện qua Gemini API. & 
- OCR trích xuất thành công văn bản thô. \newline
- Gemini bóc tách đúng thông tin kỹ năng định dạng JSON chuẩn xác. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-03 & 
1. Kích hoạt cơ chế Fallback (Keyword Parser dựa trên Regex có ranh giới từ $\backslash$b) khi giả định API Gemini bị lỗi hoặc hết quota. & 
- Hệ thống phát hiện lỗi và chuyển sang Regex thành công. \newline
- Trích xuất chính xác các kỹ năng khớp cứng trong từ điển \texttt{skills}. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
\end{tabular}
\end{center}
\end{table}

Bảng thống kê kết quả định lượng độ chính xác của mô hình sẽ được ghi nhận sau khi hoàn thành chạy thử nghiệm trên tập mẫu gồm 100 hồ sơ CV:

\begin{table}[H]
\caption{Bảng ghi nhận độ chính xác định dạng dữ liệu và NER từ mô hình}
\label{tab:ner_evaluation_specs}
\begin{center}
\begin{tabular}{|L{6cm}|C{4cm}|C{4cm}|}
\hline
\textbf{Độ đo đánh giá (Metric)} & \textbf{Mục tiêu kỳ vọng} & \textbf{Kết quả thực tế} \\
\hline
Tỷ lệ hợp lệ cấu trúc JSON & 100.0\% (sau khi retry) & \textit{[Chờ điền]} \\
\hline
Độ chính xác (Precision) của NER & $\ge 85.0\%$ & \textit{[Chờ điền]} \\
\hline
Độ phủ (Recall) của NER & $\ge 80.0\%$ & \textit{[Chờ điền]} \\
\hline
Điểm F1 (F1-Score) của NER & $\ge 82.5\%$ & \textit{[Chờ điền]} \\
\hline
\end{tabular}
\end{center}
\end{table}

\subsection{Đặc tả kịch bản kiểm thử chuẩn hóa kỹ năng và so khớp mờ doanh nghiệp}

Mục tiêu là kiểm chứng tính chính xác của việc ánh xạ thực thể bằng thuật toán lân cận gần nhất (FAISS với chỉ mục \texttt{IndexFlatIP}) và thuật toán Jaccard Similarity để gộp các công ty có tên biến thể khác nhau.

\begin{table}[H]
\caption{Ma trận kịch bản kiểm thử chuẩn hóa kỹ năng và khớp mờ doanh nghiệp}
\label{tab:tc_normalization}
\begin{center}
\begin{tabular}{|C{1.2cm}|L{4.2cm}|L{4.2cm}|L{4.2cm}|C{1.8cm}|}
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
TC-ALG-04 & 
1. Nhập nhãn kỹ năng thô trích xuất từ CV/JD (ví dụ: "postgres", "nextjs"). \newline
2. Gọi FAISS tìm kiếm lân cận trên không gian vector nhúng 384 chiều. & 
- Ánh xạ chính xác sang kỹ năng chuẩn hóa trong từ điển ("PostgreSQL" và "Next.js"). \newline
- Độ trễ tìm kiếm dưới 5ms. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-05 & 
1. Nhập tên doanh nghiệp chứa các hậu tố pháp lý khác nhau (ví dụ: "Công ty Cổ phần VNG", "VNG Corporation JSC"). \newline
2. Chạy thuật toán lọc hậu tố và so khớp Jaccard. & 
- Loại bỏ hoàn toàn các từ gây nhiễu pháp lý. \newline
- Điểm tương đồng Jaccard vượt ngưỡng và tự động gom nhóm về cùng ID công ty. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
\end{tabular}
\end{center}
\end{table}

\subsection{Đặc tả kịch bản kiểm thử thuật toán khử trùng lặp sâu bài đăng}

Mục tiêu là kiểm chứng thuật toán TF-IDF kết hợp Cosine Similarity để phát hiện và lọc trùng lặp các tin đăng giống nhau từ cùng một công ty trong vòng 30 ngày.

\begin{table}[H]
\caption{Ma trận kịch bản kiểm thử thuật toán khử trùng lặp sâu tin đăng}
\label{tab:tc_deduplication}
\begin{center}
\begin{tabular}{|C{1.2cm}|L{4.2cm}|L{4.2cm}|L{4.2cm}|C{1.8cm}|}
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
TC-ALG-06 & 
1. Nhập 2 tin tuyển dụng từ một công ty có nội dung tương đồng trên 90\% (chỉ khác ngày đăng). \newline
2. Chạy bộ khử trùng sâu TF-IDF \& Cosine. & 
- Độ tương đồng Cosine đo được $> 80\%$. \newline
- Hệ thống loại bỏ tin mới, chỉ cập nhật thời điểm thu thập \texttt{last\_seen} của tin cũ. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-07 & 
1. Nhập 2 tin tuyển dụng có vai trò khác nhau (ví dụ: "Frontend" và "Backend") của cùng một công ty. \newline
2. Chạy bộ khử trùng sâu. & 
- Độ tương đồng Cosine đo được $< 80\%$. \newline
- Hệ thống ghi nhận đây là hai bài đăng độc lập, tiến hành lưu trữ cả hai bình thường. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
\end{tabular}
\end{center}
\end{table}

\subsection{Đặc tả kịch bản kiểm thử thuật toán so khớp năng lực Matching Engine}

Mục tiêu của phân hệ này là kiểm chứng tính đúng đắn của thuật toán Matching Engine trong việc áp đặt 6 quy tắc lọc ngữ nghĩa (Semantic Filtering Rules) và tính điểm số có trọng số (Weighted Match Score) cuối cùng.

\begin{table}[H]
\caption{Ma trận kịch bản kiểm thử các quy tắc lọc và tính điểm Matching Engine}
\label{tab:tc_matching}
\begin{center}
\begin{tabular}{|C{1.2cm}|L{4.2cm}|L{4.2cm}|L{4.2cm}|C{1.8cm}|}
\hline
\textbf{ID} & \textbf{Các bước thực hiện} & \textbf{Kết quả kỳ vọng} & \textbf{Kết quả thực tế} & \textbf{Trạng thái} \\
\hline
TC-ALG-08 & 
1. Đưa vào cặp kỹ năng so khớp chứa Chứng chỉ (\texttt{type = "Certification"}) và một kỹ năng chuyên môn khác. & 
- Áp dụng quy tắc Certification Exclusivity. \newline
- Độ tương đồng bị ép cứng về $0.0$. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-09 & 
1. Đưa vào cặp kỹ năng lệch loại hình phân loại (ví dụ: \texttt{Specialized skill} và \texttt{Common skill}). & 
- Áp dụng quy tắc Type Grouping. \newline
- Độ tương đồng bị gán bằng $0.0$. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-10 & 
1. Đưa vào một kỹ năng công cụ (\texttt{is\_software = true}) và một kỹ năng chung (\texttt{Common skill}). & 
- Áp dụng quy tắc Software/Tool Exclusivity. \newline
- Độ tương đồng bị gán bằng $0.0$. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-11 & 
1. Đưa vào một kỹ năng ngôn ngữ (\texttt{is\_language = true}) và kỹ năng không phải ngôn ngữ hoặc khác gốc ngôn ngữ. & 
- Áp dụng quy tắc Language Filtering. \newline
- Độ tương đồng bị gán bằng $0.0$. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-12 & 
1. Đưa vào hai kỹ năng trùng khớp hoàn toàn phân nhóm chuyên môn (\texttt{category}). & 
- Áp dụng quy tắc Subcategory Exclusivity \& Boost. \newline
- Độ tương đồng được nhân hệ số tăng cường $1.2$: $\text{Sim}_{\text{boosted}} = \min(1.0, \text{Sim} \times 1.2)$. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-13 & 
1. Đưa vào hai kỹ năng mềm (\texttt{Common skill}) có độ tương đồng cosine $> 0.35$. & 
- Áp dụng quy tắc Soft Skills Direct Match. \newline
- Hệ thống tự động nâng độ tương đồng lên mức khớp tối đa ($1.0$). & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
TC-ALG-14 & 
1. Nhập danh sách kỹ năng CV và JD kèm trọng số $W$. \newline
2. Gọi hàm tính điểm Matching Score. & 
- Kết quả điểm số tính toán chính xác theo trung bình tương đồng có trọng số. \newline
- Phân loại đúng kỹ năng vào \texttt{radar\_data} và \texttt{gap\_report}. & 
\textit{[Chưa thực hiện]} & 
\textit{[Chờ test]} \\
\hline
\end{tabular}
\end{center}
\end{table}

Bảng phân tích đối soát chi tiết thực tế của một trường hợp thử nghiệm cụ thể (Case Study) sẽ được điền đầy đủ số liệu sau khi hoàn thành chạy thực thi thuật toán:

\begin{table}[H]
\caption{Biểu mẫu phân tích chi tiết kết quả so khớp và gợi ý kỹ năng thực tế (Case Study)}
\label{tab:matching_case_study_template}
\begin{center}
\begin{tabular}{|L{3.5cm}|C{2.8cm}|C{2.8cm}|L{5.0cm}|}
\hline
\textbf{Kỹ năng yêu cầu} & \textbf{Trạng thái khớp} & \textbf{Hệ số tương đồng} & \textbf{Giải thích / Gợi ý hành động} \\
\hline
Python Programming & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
SQL Database & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
Teamwork & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
Pandas & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
Power BI & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
Git & \textit{[Chờ test]} & \textit{[Chờ điền]} & \textit{[Chờ phân tích kết quả thực tế]} \\
\hline
\end{tabular}
\end{center}
\end{table}

Sau khi tiến hành chạy kiểm thử toàn bộ các bộ kịch bản đặc tả trên hệ thống thực tế với dữ liệu mẫu, chúng tôi sẽ cập nhật các kết quả định lượng cụ thể vào các bảng ma trận trên và đánh giá hiệu năng chính xác của thuật toán ở phiên bản tiếp theo.

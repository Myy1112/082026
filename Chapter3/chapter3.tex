\chapter{Phân tích và thiết kế hệ thống}
\label{Chapter3}

% ================================================================
\section{Phân tích yêu cầu hệ thống}

% ----------------------------------------------------------------
\subsection{Yêu cầu nghiệp vụ}

Sinh viên ngành Công nghệ thông tin tại Việt Nam thường đối mặt với một nghịch lý trong quá trình tìm kiếm việc làm: dù sở hữu bằng cấp chuyên môn, nhiều sinh viên không có khả năng đánh giá khách quan khoảng cách giữa năng lực bản thân và yêu cầu thực tế của thị trường tuyển dụng. Các công cụ hỗ trợ hiện có, chủ yếu dưới dạng các trang tuyển dụng thông thường, chỉ cung cấp danh sách tin đăng mà thiếu hẳn cơ chế phân tích và phản hồi cá nhân hóa giúp sinh viên nhận diện điểm yếu năng lực một cách có hệ thống.

Bài toán nghiệp vụ cốt lõi của hệ thống CareerNova được xác định như sau: sinh viên CNTT cần một công cụ tự động hóa việc so sánh hồ sơ năng lực của bản thân với tập hợp yêu cầu kỹ năng từ các tin tuyển dụng thực tế trên thị trường, từ đó cung cấp kết quả so khớp trực quan và có thể hành động được. Để giải quyết bài toán này, hệ thống hoạt động theo luồng nghiệp vụ khép kín gồm bốn giai đoạn chính:

\textbf{(1) Thu thập dữ liệu tuyển dụng:} Hệ thống ETL tự động crawl và trích xuất thông tin từ các nền tảng tuyển dụng lớn tại Việt Nam (ITViec, VietnamWorks, CareerViet, LinkedIn), bao gồm yêu cầu kỹ năng, mức lương và thông tin ngành nghề, nhằm xây dựng kho dữ liệu thị trường được cập nhật liên tục.

\textbf{(2) Phân tích và chuẩn hóa:} Dữ liệu tuyển dụng được làm sạch, trích xuất thực thể bằng Gemini API và chuẩn hóa ngữ nghĩa về một từ điển kỹ năng thống nhất, đảm bảo tính nhất quán để so sánh chéo giữa các nguồn thu thập.

\textbf{(3) So khớp CV với thị trường:} Khi sinh viên tải lên CV, hệ thống tự động bóc tách kỹ năng, tính điểm tương đồng ngữ nghĩa dựa trên mô hình Sentence-BERT kết hợp trọng số TF-IDF thị trường, rồi tạo ra báo cáo khoảng cách kỹ năng và biểu đồ Radar chi tiết.

\textbf{(4) Trực quan hóa và hỗ trợ quyết định:} Kết quả được hiển thị qua giao diện dashboard tương tác, giúp sinh viên đọc hiểu tình trạng năng lực, theo dõi xu hướng tuyển dụng thị trường và nhận đề xuất lộ trình học tập cá nhân hóa.

Hai nhóm đối tượng sử dụng chính được xác định: \textbf{Sinh viên CNTT} (người dùng cuối chính, sử dụng toàn bộ tính năng phân tích và trực quan hóa) và \textbf{Quản trị viên} (Admin, có quyền truy cập nâng cao vào dữ liệu hệ thống và quản lý tài khoản người dùng). Ngoài ra, \textbf{Hệ thống ETL} đóng vai trò tác nhân tự động vận hành luồng thu thập và chuẩn hóa dữ liệu nền tảng.

% ----------------------------------------------------------------
\subsection{Yêu cầu chức năng}

Các yêu cầu chức năng được xác định dựa trên cấu trúc route thực tế của ứng dụng Frontend (Next.js) và các module API của Backend (NestJS), được phân nhóm theo từng Actor như trong Bảng~\ref{tab:functional_requirements}.

\begin{longtable}{|L{1.5cm}|L{2.8cm}|L{9.2cm}|}
\caption{Danh sách yêu cầu chức năng của hệ thống CareerNova.}
\label{tab:functional_requirements} \\
\hline
\textbf{Mã} & \textbf{Actor} & \textbf{Mô tả} \\
\hline
\endfirsthead
\hline
\textbf{Mã} & \textbf{Actor} & \textbf{Mô tả} \\
\hline
\endhead
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Quản lý tài khoản}} \\
\hline
UC-01 & Sinh viên & Đăng ký tài khoản mới bằng địa chỉ email và mật khẩu. \\
\hline
UC-02 & Sinh viên & Đăng nhập bằng tài khoản email/mật khẩu hoặc Google OAuth (Single Sign-On). \\
\hline
UC-03 & Sinh viên & Xác thực địa chỉ email sau khi đăng ký để kích hoạt tài khoản. \\
\hline
UC-04 & Sinh viên & Yêu cầu đặt lại mật khẩu qua email và tạo mật khẩu mới. \\
\hline
UC-05 & Sinh viên & Hoàn tất luồng onboarding: khai báo thông tin năng lực và định hướng nghề nghiệp ban đầu. \\
\hline
UC-06 & Sinh viên & Cập nhật hồ sơ cá nhân (họ tên, ảnh đại diện, thông tin nghề nghiệp). \\
\hline
UC-07 & Sinh viên & Cài đặt tài khoản: thay đổi giao diện sáng/tối, quản lý thông báo, xóa tài khoản. \\
\hline
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Quản lý CV và So khớp}} \\
\hline
UC-08 & Sinh viên & Tải lên, quản lý và đặt CV mặc định để sử dụng trong so khớp. \\
\hline
UC-09 & Sinh viên & Thực hiện so khớp CV với tin tuyển dụng: hệ thống tính điểm tương đồng và tạo kết quả phân tích. \\
\hline
UC-10 & Sinh viên & Xem biểu đồ Radar thể hiện điểm kỹ năng bản thân so với yêu cầu thị trường. \\
\hline
UC-11 & Sinh viên & Xem Gap Report: báo cáo chi tiết kỹ năng đạt yêu cầu, đạt một phần và còn thiếu hụt. \\
\hline
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Khám phá việc làm}} \\
\hline
UC-12 & Sinh viên & Tìm kiếm và lọc tin tuyển dụng theo từ khóa, kỹ năng, mức lương và địa điểm. \\
\hline
UC-13 & Sinh viên & Xem chi tiết tin tuyển dụng bao gồm mô tả công việc và yêu cầu kỹ năng. \\
\hline
UC-14 & Sinh viên & Lưu tin tuyển dụng yêu thích vào danh sách cá nhân. \\
\hline
UC-15 & Sinh viên & Xem danh sách việc làm được đề xuất dựa trên hồ sơ năng lực cá nhân. \\
\hline
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Phân tích và Trực quan hóa}} \\
\hline
UC-16 & Sinh viên & Xem Dashboard thị trường: xu hướng đăng tuyển, phân bổ ngành công nghiệp và dải lương theo vị trí. \\
\hline
UC-17 & Sinh viên & Xem Dashboard cá nhân: tổng hợp năng lực, tiến độ và kết quả so khớp của bản thân. \\
\hline
UC-18 & Sinh viên & Xem lộ trình học tập được đề xuất để lấp đầy khoảng cách kỹ năng đã phát hiện. \\
\hline
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Quản trị hệ thống}} \\
\hline
UC-19 & Admin & Xem và quản lý danh sách người dùng trong hệ thống (xem thông tin, phân quyền). \\
\hline
UC-20 & Admin & Xem báo cáo tổng thể về hoạt động và dữ liệu của hệ thống. \\
\hline
\multicolumn{3}{|l|}{\textit{Nhóm chức năng: Thu thập và xử lý dữ liệu (tự động)}} \\
\hline
UC-21 & Hệ thống ETL & Thu thập dữ liệu tin tuyển dụng tự động từ các nền tảng ITViec, VietnamWorks, CareerViet, LinkedIn. \\
\hline
UC-22 & Hệ thống ETL & Chuẩn hóa, trích xuất thực thể kỹ năng và nạp dữ liệu vào cơ sở dữ liệu PostgreSQL. \\
\hline
\end{longtable}

% ----------------------------------------------------------------
\subsection{Yêu cầu phi chức năng}

Các yêu cầu phi chức năng được suy ra từ các tệp cấu hình kỹ thuật thực tế của hệ thống, bao gồm \texttt{Dockerfile} của Frontend (Node 20-Alpine, port 3000) và Backend (Node 20-Alpine, port 3000, OpenSSL), tệp \texttt{docker-compose.yml} (PostgreSQL 17, Docker volume), và tệp \texttt{requirements.txt} của ETL Pipeline (FastAPI, Torch, Sentence-transformers, FAISS-cpu, Psycopg2). Các yêu cầu được phân loại và trình bày trong Bảng~\ref{tab:nonfunctional_requirements}.

\begin{longtable}{|L{1.5cm}|L{2.8cm}|L{9.2cm}|}
\caption{Danh sách yêu cầu phi chức năng của hệ thống CareerNova.}
\label{tab:nonfunctional_requirements} \\
\hline
\textbf{Mã} & \textbf{Loại} & \textbf{Mô tả} \\
\hline
\endfirsthead
\hline
\textbf{Mã} & \textbf{Loại} & \textbf{Mô tả} \\
\hline
\endhead
NFR-01 & Hiệu năng & Thời gian phản hồi của API Backend không vượt quá 2 giây cho 95\% các yêu cầu truy vấn thông thường (dashboard, tìm kiếm, hồ sơ). \\
\hline
NFR-02 & Hiệu năng & Toàn bộ quy trình so khớp CV (bóc tách kỹ năng qua Gemini API, tính điểm SBERT, tạo Gap Report) hoàn thành trong vòng 30 giây kể từ thời điểm người dùng gửi yêu cầu. \\
\hline
NFR-03 & Hiệu năng & Tìm kiếm ngữ nghĩa trên chỉ mục vector FAISS đạt độ trễ dưới 100ms cho tập dữ liệu hàng triệu bản ghi, nhờ cơ chế tính trước (precompute) và lưu cache vector nhúng. \\
\hline
NFR-04 & Bảo mật & Xác thực người dùng được thực hiện theo cơ chế JWT stateless; access token có thời hạn ngắn và được làm mới qua refresh token để giảm thiểu rủi ro rò rỉ. \\
\hline
NFR-05 & Bảo mật & Mật khẩu người dùng được băm bằng thuật toán bcrypt trước khi lưu vào cơ sở dữ liệu; không lưu trữ mật khẩu dạng plaintext tại bất kỳ tầng nào. \\
\hline
NFR-06 & Bảo mật & Các container Docker chạy dưới tài khoản non-root (UID 1001 đối với Frontend) và không nhúng thông tin xác thực nhạy cảm vào image, tuân thủ nguyên tắc đặc quyền tối thiểu. \\
\hline
NFR-07 & Bảo mật & Toàn bộ giao tiếp giữa Frontend và Backend được mã hóa qua HTTPS/TLS; Backend tích hợp OpenSSL (khai báo trong Dockerfile) để hỗ trợ kết nối cơ sở dữ liệu bảo mật. \\
\hline
NFR-08 & Khả năng mở rộng & Mỗi thành phần của hệ thống (Frontend, Backend API, ETL Pipeline) được đóng gói độc lập trong Docker container, cho phép triển khai, nâng cấp và mở rộng theo chiều ngang (horizontal scaling) mà không ảnh hưởng đến các thành phần còn lại. \\
\hline
NFR-09 & Khả năng mở rộng & Pipeline ETL được thiết kế theo kiến trúc module hóa (crawl → clean → normalize → import), cho phép bổ sung nguồn tuyển dụng mới hoặc thay thế mô hình LLM mà không cần thay đổi cấu trúc tổng thể. \\
\hline
NFR-10 & Độ tin cậy & Dữ liệu PostgreSQL được lưu trữ bền vững qua Docker named volume (\texttt{career\_nova\_postgres\_data}), đảm bảo dữ liệu không bị mất khi container khởi động lại; container PostgreSQL cấu hình \texttt{restart: unless-stopped}. \\
\hline
NFR-11 & Độ tin cậy & ETL Pipeline tích hợp cơ chế fallback tự động: ưu tiên cào tĩnh bằng Requests/cloudscraper, tự động chuyển sang Selenium Headless Browser khi gặp tường lửa chống bot, đảm bảo tính liên tục của quá trình thu thập dữ liệu. \\
\hline
NFR-12 & Độ tin cậy & Kết quả bóc tách kỹ năng từ LLM (Gemini API) được kiểm chứng bằng cơ chế anti-hallucination: mỗi thực thể kỹ năng phải kèm bằng chứng (\texttt{evidence\_text}) trích từ văn bản gốc; nếu không khớp sẽ bị loại bỏ tự động. \\
\hline
NFR-13 & Bảo trì & Backend NestJS được tổ chức theo kiến trúc module hóa rõ ràng (auth, profile, job, matching, skill-gap, market-dashboard, v.v.), mỗi module độc lập về controller, service và DTO, giúp dễ dàng bảo trì và mở rộng từng tính năng riêng lẻ. \\
\hline
NFR-14 & Bảo trì & Toàn bộ Frontend và Backend sử dụng TypeScript với kiểu dữ liệu tường minh, giúp phát hiện lỗi tại thời điểm biên dịch, giảm thiểu lỗi runtime và hỗ trợ tái cấu trúc mã nguồn an toàn. \\
\hline
\end{longtable}

% ================================================================
\section{Thiết kế hệ thống}

% ----------------------------------------------------------------
\subsection{Sơ đồ kiến trúc tổng thể}

Hệ thống CareerNova được xây dựng theo kiến trúc đa tầng (multi-tier architecture) với bốn tầng chức năng độc lập, mỗi tầng đảm nhiệm một vai trò chuyên biệt trong luồng xử lý dữ liệu từ nguồn tuyển dụng đến giao diện người dùng cuối. Sơ đồ kiến trúc tổng thể được thể hiện trong Hình~\ref{fig:system_architecture}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{system_architecture}
    \caption{Sơ đồ kiến trúc tổng thể hệ thống CareerNova.}
    \label{fig:system_architecture}
\end{figure}

\textbf{Tầng Trình Bày (Presentation Layer)} bao gồm ứng dụng web đơn trang được xây dựng trên Next.js 15 và React 19 (TypeScript). Tầng này đảm nhận toàn bộ việc hiển thị giao diện và tương tác người dùng, bao gồm các trang xác thực (đăng nhập, đăng ký, Google OAuth), luồng onboarding, trang quản lý CV, và bốn thành phần trực quan hóa cốt lõi: \textit{MarketDashboard} (AreaChart, PieChart, BarChart), \textit{SkillGapAnalysis} (RadarChart, Gap Report), \textit{PersonalDashboard} và \textit{LearningRoadmap}. Giao tiếp với tầng nghiệp vụ được thực hiện qua REST API sử dụng HTTPS và xác thực JWT.

\textbf{Tầng Nghiệp Vụ (Business Logic Layer)} gồm hai thành phần hoạt động phối hợp. Thành phần thứ nhất là \textbf{CareerNova Backend API} (NestJS, Node 20), đóng vai trò cổng dịch vụ điều phối toàn bộ luồng yêu cầu từ Frontend: quản lý xác thực và phân quyền (JWT), xử lý hồ sơ người dùng, tổng hợp dữ liệu dashboard, và điều phối luồng so khớp CV sang ETL Service qua giao thức HTTP. Kết nối cơ sở dữ liệu được thực hiện qua Prisma ORM. Thành phần thứ hai là \textbf{ETL Pipeline} (Python, FastAPI), chịu trách nhiệm toàn bộ luồng xử lý dữ liệu nặng: thu thập JD từ các nền tảng tuyển dụng bằng Selenium và Requests/cloudscraper, trích xuất và chuẩn hóa thực thể kỹ năng qua Gemini API, tính điểm so khớp ngữ nghĩa bằng SBERT và FAISS, rồi nạp kết quả vào PostgreSQL qua SQLAlchemy.

\textbf{Tầng Dữ Liệu (Data Layer)} sử dụng PostgreSQL 17, lưu trữ toàn bộ dữ liệu hệ thống: thông tin người dùng và CV (\texttt{users}, \texttt{user\_cvs}), kho dữ liệu tuyển dụng (\texttt{jobs}, \texttt{skills}, \texttt{job\_skills}, \texttt{salaries}, \texttt{industries}), và kết quả phân tích so khớp (\texttt{cv\_job\_matches} với hai cột JSONB \texttt{radar\_data} và \texttt{gap\_report}). Việc lưu trữ trước kết quả tính toán dưới dạng JSONB giúp tối ưu hóa hiệu năng truy vấn thời gian thực tại tầng trình bày mà không cần tính toán lại.

\textbf{Tầng Dịch Vụ Ngoài (External Services Layer)} bao gồm ba thành phần: \textbf{Google Gemini API} thực hiện trích xuất thực thể kỹ năng có cấu trúc từ văn bản JD và CV (Structured Outputs với JSON Schema); \textbf{mô hình nhúng all-MiniLM-L6-v2} (Sentence Transformers) mã hóa kỹ năng thành vector 384 chiều và xây dựng chỉ mục FAISS để tìm kiếm ngữ nghĩa; và \textbf{các nền tảng tuyển dụng} (ITViec, VietnamWorks, CareerViet, LinkedIn) cung cấp nguồn dữ liệu JD thô cho ETL Pipeline.

% ----------------------------------------------------------------
\subsection{Sơ đồ ca sử dụng tổng quát}

Sơ đồ ca sử dụng tổng quát thể hiện toàn bộ tương tác giữa ba Actor --- Sinh viên, Admin và Hệ thống ETL --- với hệ thống CareerNova, bao gồm các quan hệ \texttt{include} và \texttt{extend} giữa các ca sử dụng. Sơ đồ được mô tả trong Hình~\ref{fig:usecase_diagram}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{usecase_diagram}
    \caption{Sơ đồ ca sử dụng tổng quát của hệ thống CareerNova.}
    \label{fig:usecase_diagram}
\end{figure}

Bảng~\ref{tab:uc09}, Bảng~\ref{tab:uc11} và Bảng~\ref{tab:uc16} trình bày đặc tả chi tiết cho ba ca sử dụng quan trọng nhất của hệ thống.

\begin{longtable}{|L{3.5cm}|L{10cm}|}
\caption{Đặc tả ca sử dụng UC-09: Tải lên CV và So khớp kỹ năng.}
\label{tab:uc09} \\
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endfirsthead
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endhead
Mã & UC-09 \\
\hline
Tên & Tải lên CV và So khớp kỹ năng với thị trường \\
\hline
Actor chính & Sinh viên \\
\hline
Tiền điều kiện & Sinh viên đã đăng nhập và hoàn tất onboarding; tài khoản đang active. \\
\hline
Luồng chính & 1. Sinh viên truy cập trang CV Matching, chọn tệp CV (PDF/DOCX) và tin tuyển dụng mục tiêu. \newline 2. Hệ thống xác thực định dạng tệp, tải lên Backend API. \newline 3. Backend API lưu metadata CV vào \texttt{user\_cvs}, chuyển tiếp sang ETL Matching Service. \newline 4. ETL Service trích xuất văn bản CV (OCR nếu là ảnh quét), gọi Gemini API bóc tách danh sách kỹ năng. \newline 5. Hệ thống mã hóa kỹ năng thành vector SBERT, tính Match Score = $\sum(\text{weight} \times \text{cosine}) \div \sum(\text{weight})$. \newline 6. Kết quả \texttt{radar\_data} và \texttt{gap\_report} JSONB được lưu vào bảng \texttt{cv\_job\_matches}. \newline 7. Frontend truy vấn và hiển thị biểu đồ Radar và Gap Report. \\
\hline
Luồng thay thế & 4a. Nếu CV là ảnh quét: hệ thống thực hiện OCR (pdf2image + Pytesseract) trước khi trích xuất. \newline 4b. Nếu kỹ năng được Gemini trả về không có \texttt{evidence\_text} khớp trong văn bản gốc: thực thể bị loại bỏ (anti-hallucination). \\
\hline
Hậu điều kiện & Kết quả so khớp được lưu vào cơ sở dữ liệu; sinh viên xem được biểu đồ Radar và danh sách kỹ năng còn thiếu. \\
\hline
\end{longtable}

\begin{longtable}{|L{3.5cm}|L{10cm}|}
\caption{Đặc tả ca sử dụng UC-11: Xem Gap Report và báo cáo khoảng cách kỹ năng.}
\label{tab:uc11} \\
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endfirsthead
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endhead
Mã & UC-11 \\
\hline
Tên & Xem Gap Report và báo cáo khoảng cách kỹ năng \\
\hline
Actor chính & Sinh viên \\
\hline
Tiền điều kiện & Sinh viên đã thực hiện so khớp CV (UC-09); trường \texttt{gap\_report} JSONB đã được lưu vào \texttt{cv\_job\_matches}. \\
\hline
Luồng chính & 1. Sinh viên truy cập trang Skill Gap Analysis. \newline 2. Frontend gọi API lấy dữ liệu từ \texttt{cv\_job\_matches.gap\_report}. \newline 3. Hệ thống phân loại kỹ năng thành ba nhóm: Đạt yêu cầu (xanh lá), Đạt một phần (vàng), Thiếu hụt (đỏ). \newline 4. Frontend hiển thị biểu đồ cột ngang phân kỳ và danh sách kỹ năng chi tiết. \newline 5. Sinh viên mở rộng từng danh mục kỹ năng để xem điểm \texttt{user\_rate} và \texttt{market\_rate}. \\
\hline
Luồng thay thế & 2a. Nếu chưa có kết quả so khớp: hệ thống hiển thị gợi ý thực hiện UC-09 trước. \\
\hline
Hậu điều kiện & Sinh viên nắm được danh sách kỹ năng cụ thể cần bổ sung và có thể điều hướng sang UC-18 để xem lộ trình học tập đề xuất. \\
\hline
\end{longtable}

\begin{longtable}{|L{3.5cm}|L{10cm}|}
\caption{Đặc tả ca sử dụng UC-16: Xem Dashboard thống kê thị trường việc làm.}
\label{tab:uc16} \\
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endfirsthead
\hline
\textbf{Trường} & \textbf{Nội dung} \\
\hline
\endhead
Mã & UC-16 \\
\hline
Tên & Xem Dashboard thống kê thị trường việc làm \\
\hline
Actor chính & Sinh viên \\
\hline
Tiền điều kiện & Sinh viên đã đăng nhập; cơ sở dữ liệu đã có dữ liệu tuyển dụng từ ETL Pipeline. \\
\hline
Luồng chính & 1. Sinh viên truy cập trang Market Dashboard. \newline 2. Frontend khởi tạo fetchDashboardData() và gửi đồng thời (Promise.all) ba yêu cầu API: xu hướng tuyển dụng, phân bổ ngành và dải lương. \newline 3. Backend API truy vấn PostgreSQL và trả về JSON tương ứng cho từng biểu đồ. \newline 4. Frontend kết xuất AreaChart (xu hướng), PieChart (phân bổ ngành) và BarChart (dải lương) bằng Recharts. \newline 5. Sinh viên lọc theo khoảng thời gian (tháng/năm) để xem xu hướng tương ứng. \\
\hline
Luồng thay thế & 3a. Nếu không có dữ liệu trong cơ sở dữ liệu (ETL chưa chạy): hiển thị thông báo dữ liệu chưa sẵn sàng. \\
\hline
Hậu điều kiện & Sinh viên nắm được xu hướng tuyển dụng, các ngành đang có nhu cầu cao và mức lương tham chiếu trên thị trường. \\
\hline
\end{longtable}

% ----------------------------------------------------------------
\subsection{Sơ đồ tuần tự cho các chức năng chính}

\subsubsection*{Luồng tải lên CV và So khớp kỹ năng}

Hình~\ref{fig:cv_upload_sequence} mô tả luồng tương tác tuần tự giữa các thành phần hệ thống khi sinh viên thực hiện chức năng tải lên CV và so khớp kỹ năng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{cv_upload_sequence}
    \caption{Sơ đồ tuần tự luồng tải lên CV và so khớp kỹ năng.}
    \label{fig:cv_upload_sequence}
\end{figure}

Luồng xử lý bắt đầu khi người dùng gửi tệp CV từ Frontend. Backend API tiếp nhận, lưu metadata và chuyển tiếp sang ETL Matching Service qua HTTP. Tại đây, văn bản CV được bóc tách (OCR nếu cần), kỹ năng được trích xuất qua Gemini API với cơ chế kiểm chứng chống ảo giác (\texttt{evidence\_text}), sau đó mã hóa thành vector 384 chiều bằng mô hình SBERT và tính điểm tương đồng Cosine có trọng số TF-IDF thị trường. Kết quả \texttt{radar\_data} và \texttt{gap\_report} JSONB được lưu vào bảng \texttt{cv\_job\_matches} trong PostgreSQL và trả về Frontend để kết xuất biểu đồ Radar và Gap Report.

\subsubsection*{Luồng xem Dashboard thị trường}

Hình~\ref{fig:dashboard_sequence} mô tả luồng tương tác tuần tự khi sinh viên truy cập trang Dashboard thị trường.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{dashboard_sequence}
    \caption{Sơ đồ tuần tự luồng xem Dashboard thống kê thị trường việc làm.}
    \label{fig:dashboard_sequence}
\end{figure}

Khi người dùng điều hướng đến MarketDashboard, Frontend khởi tạo ba truy vấn API song song (Promise.all) để lấy dữ liệu xu hướng tuyển dụng, phân bổ ngành và dải lương. Backend API thực hiện các truy vấn tổng hợp (aggregation) trực tiếp trên PostgreSQL và trả về JSON cho từng biểu đồ. Frontend cập nhật state qua \texttt{setTrends()}, \texttt{setIndustriesData()} và \texttt{setSalaryRangesData()}, rồi kết xuất đồng thời AreaChart, PieChart và BarChart thông qua thư viện Recharts, mang lại trải nghiệm tải trang nhanh và phản hồi tương tác tức thì.

% ================================================================
\section{Thiết kế cơ sở dữ liệu}

Cơ sở dữ liệu của hệ thống được xây dựng trên hệ quản trị cơ sở dữ liệu quan hệ PostgreSQL (phiên bản 17). Thiết kế đảm bảo tính toàn vẹn dữ liệu, hiệu năng truy vấn thông qua các ràng buộc khóa ngoại (foreign key), chỉ mục (index) và phân tách chuẩn hóa các thực thể.

% ----------------------------------------------------------------
\subsection{Sơ đồ mối quan hệ thực thể (ERD)}

Dưới đây là sơ đồ mối quan hệ thực thể (ERD) biểu diễn cấu trúc cơ sở dữ liệu của hệ thống:

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{image3_1}
    \caption{Sơ đồ mối quan hệ thực thể (ERD) của hệ thống.}
    \label{fig:erd}
\end{figure}

% ----------------------------------------------------------------
\subsection{Đặc tả các bảng dữ liệu thực tế}

Dưới đây là chi tiết cấu trúc thuộc tính, kiểu dữ liệu, các ràng buộc và ý nghĩa chức năng của từng bảng trong cơ sở dữ liệu.

% --- Bảng 1: users ---
\subsubsection*{1. Bảng \texttt{users} (Thông tin người dùng)}

Bảng này lưu trữ thông tin tài khoản người dùng, bao gồm sinh viên và quản trị viên hệ thống.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{users}.}
\label{tab:users} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{user\_id} & uuid & PK, DEFAULT gen\_random\_uuid() & Định danh duy nhất của người dùng. \\
\hline
\texttt{full\_name} & varchar(255) & NULL & Họ và tên đầy đủ. \\
\hline
\texttt{email} & varchar(255) & UNIQUE, NOT NULL & Địa chỉ email đăng ký tài khoản. \\
\hline
\texttt{password\_hash} & text & NULL (nếu đăng nhập mạng xã hội) & Mật khẩu tài khoản đã băm an toàn. \\
\hline
\texttt{avatar\_url} & text & NULL & Đường dẫn ảnh đại diện. \\
\hline
\texttt{role} & varchar(50) & DEFAULT 'student', CHECK (role IN ('student', 'admin')) & Vai trò tài khoản (Sinh viên / Quản trị viên). \\
\hline
\texttt{created\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm tạo tài khoản. \\
\hline
\texttt{is\_active} & boolean & DEFAULT false & Trạng thái kích hoạt tài khoản. \\
\hline
\texttt{verify\_token} & varchar(255) & NULL & Mã xác thực email kích hoạt. \\
\hline
\texttt{verify\_token\_expires} & timestamp & NULL & Hạn dùng của mã xác thực email. \\
\hline
\end{longtable}

% --- Bảng 2: user_auth_providers ---
\subsubsection*{2. Bảng \texttt{user\_auth\_providers} (Liên kết nhà cung cấp xác thực)}

Lưu trữ thông tin xác thực bên thứ ba (Google, Facebook) phục vụ đăng nhập Single Sign-On (SSO).

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{user\_auth\_providers}.}
\label{tab:auth_providers} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{auth\_provider\_id} & uuid & PK, DEFAULT gen\_random\_uuid() & Định danh duy nhất liên kết xác thực. \\
\hline
\texttt{user\_id} & uuid & FK -> users(user\_id) ON DELETE CASCADE & ID người dùng sở hữu liên kết. \\
\hline
\texttt{provider} & varchar(50) & CHECK (provider IN ('local', 'google', 'facebook')) & Tên nhà cung cấp dịch vụ xác thực. \\
\hline
\texttt{provider\_user\_id} & varchar(255) & NOT NULL & ID định danh từ phía nhà cung cấp. \\
\hline
\texttt{provider\_email} & varchar(255) & NULL & Email trả về từ nhà cung cấp. \\
\hline
\texttt{provider\_name} & varchar(255) & NULL & Họ tên trả về từ nhà cung cấp. \\
\hline
\texttt{provider\_avatar\_url} & text & NULL & Ảnh đại diện trả về từ nhà cung cấp. \\
\hline
\texttt{created\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm liên kết. \\
\hline
\texttt{last\_login\_at} & timestamp & NULL & Lần đăng nhập cuối qua provider này. \\
\hline
\end{longtable}

% --- Bảng 3: user_cvs ---
\subsubsection*{3. Bảng \texttt{user\_cvs} (Thông tin tệp CV đã tải lên)}

Lưu trữ thông tin các tệp tin CV do sinh viên tải lên hệ thống.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{user\_cvs}.}
\label{tab:user_cvs} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{cv\_id} & uuid & PK, DEFAULT gen\_random\_uuid() & Định danh duy nhất cho CV. \\
\hline
\texttt{user\_id} & uuid & FK -> users(user\_id) ON DELETE CASCADE & Định danh sinh viên sở hữu CV. \\
\hline
\texttt{file\_name} & varchar(255) & NULL & Tên tệp tin gốc tải lên. \\
\hline
\texttt{file\_url} & text & NULL & Đường dẫn URL tệp tin trên hệ thống lưu trữ. \\
\hline
\texttt{extracted\_text} & text & NULL & Nội dung văn bản thô trích xuất từ CV. \\
\hline
\texttt{uploaded\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm tải lên hệ thống. \\
\hline
\texttt{updated\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm cập nhật cuối cùng. \\
\hline
\end{longtable}

% --- Bảng 4: skills ---
\subsubsection*{4. Bảng \texttt{skills} (Từ điển kỹ năng hệ thống)}

Lưu trữ danh mục kỹ năng chuẩn hóa dựa trên phân loại hệ thống (ví dụ: chuẩn O*NET hoặc Lightcast).

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{skills}.}
\label{tab:skills} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{skill\_id} & integer & PK, SERIAL & Định danh duy nhất cho kỹ năng. \\
\hline
\texttt{skill\_name} & varchar(255) & UNIQUE, NOT NULL & Tên chuẩn hóa của kỹ năng. \\
\hline
\texttt{category} & varchar(255) & NULL & Nhóm phân loại chính của kỹ năng. \\
\hline
\texttt{type} & varchar(100) & NULL & Loại kỹ năng (Hard Skill, Common Skill). \\
\hline
\texttt{created\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Ngày tạo bản ghi kỹ năng. \\
\hline
\end{longtable}

% --- Bảng 5: user_cv_skills ---
\subsubsection*{5. Bảng \texttt{user\_cv\_skills} (Kỹ năng chi tiết của sinh viên)}

Bảng cầu nối lưu trữ danh sách các kỹ năng chuẩn hóa được trích xuất từ CV ứng viên.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{user\_cv\_skills}.}
\label{tab:user_cv_skills} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{cv\_id} & uuid & PK, FK -> user\_cvs(cv\_id) ON DELETE CASCADE & ID của CV được phân tích. \\
\hline
\texttt{skill\_id} & integer & PK, FK -> skills(skill\_id) ON DELETE CASCADE & ID kỹ năng chuẩn hóa tương ứng. \\
\hline
\texttt{raw\_skill} & varchar(255) & NULL & Cụm từ kỹ năng gốc ghi trên CV. \\
\hline
\texttt{created\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm lưu bản ghi. \\
\hline
\end{longtable}

% --- Bảng 6: companies ---
\subsubsection*{6. Bảng \texttt{companies} (Thông tin doanh nghiệp tuyển dụng)}

Lưu trữ dữ liệu hồ sơ công ty đăng tuyển việc làm.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{companies}.}
\label{tab:companies} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{company\_id} & bigint & PK & Định danh duy nhất cho doanh nghiệp. \\
\hline
\texttt{name} & varchar(255) & NOT NULL & Tên công ty/doanh nghiệp. \\
\hline
\texttt{description} & text & NULL & Mô tả chi tiết về doanh nghiệp. \\
\hline
\texttt{company\_size\_min} & integer & NULL & Quy mô nhân sự tối thiểu. \\
\hline
\texttt{company\_size\_max} & integer & NULL & Quy mô nhân sự tối đa. \\
\hline
\texttt{country} & varchar(100) & NULL & Quốc gia đặt trụ sở. \\
\hline
\texttt{city} & varchar(100) & NULL & Thành phố đặt văn phòng. \\
\hline
\texttt{address} & text & NULL & Địa chỉ chi tiết. \\
\hline
\texttt{url} & varchar(500) & NULL & Liên kết trang web chính thức. \\
\hline
\texttt{industry} & varchar(255) & NULL & Lĩnh vực hoạt động chính. \\
\hline
\end{longtable}

% --- Bảng 7: jobs ---
\subsubsection*{7. Bảng \texttt{jobs} (Thông tin việc làm đăng tuyển)}

Lưu thông tin chi tiết các bản tin tuyển dụng việc làm thu thập được.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{jobs}.}
\label{tab:jobs} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{job\_id} & bigint & PK, SERIAL & Định danh tin tuyển dụng. \\
\hline
\texttt{company\_id} & bigint & FK -> companies(company\_id) ON DELETE SET NULL & ID công ty đăng tuyển. \\
\hline
\texttt{title} & varchar(500) & NOT NULL & Tiêu đề công việc đăng tuyển. \\
\hline
\texttt{skills\_desc} & text & NULL & Phần mô tả yêu cầu kỹ năng. \\
\hline
\texttt{description} & text & NULL & Mô tả chi tiết nhiệm vụ công việc. \\
\hline
\texttt{formatted\_experience\_level} & varchar(100) & NULL, INDEX & Cấp bậc kinh nghiệm yêu cầu. \\
\hline
\texttt{work\_type} & varchar(100) & NULL & Hình thức làm việc (Full-time/Part-time). \\
\hline
\texttt{location} & varchar(255) & NULL & Địa điểm làm việc. \\
\hline
\texttt{is\_remote} & boolean & DEFAULT false & Cho phép làm việc từ xa (Remote). \\
\hline
\texttt{listed\_time} & timestamp with time zone & NULL & Ngày đăng tin tuyển dụng. \\
\hline
\texttt{expiry\_time} & timestamp with time zone & NULL & Hạn nộp hồ sơ. \\
\hline
\texttt{job\_posting\_url} & text & NULL & Liên kết nguồn của tin tuyển dụng. \\
\hline
\texttt{scraped\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm thu thập dữ liệu. \\
\hline
\texttt{applies} & integer & DEFAULT 0 & Số lượt nộp hồ sơ ghi nhận. \\
\hline
\texttt{views} & integer & DEFAULT 0 & Số lượt xem tin tuyển dụng. \\
\hline
\texttt{fingerprint} & varchar(32) & UNIQUE & Mã MD5 hash nhận diện chống trùng lặp. \\
\hline
\texttt{job\_category} & varchar(100) & NULL & Phân mục nghề nghiệp tổng quan. \\
\hline
\texttt{search\_group} & varchar(100) & NULL & Nhóm ngành công việc tiêu chuẩn liên kết. \\
\hline
\texttt{source\_name} & varchar(50) & NULL & Nguồn thu thập (LinkedIn, Vietnamworks...). \\
\hline
\texttt{source\_id} & varchar(255) & NULL & ID tin gốc trên nguồn thu thập. \\
\hline
\end{longtable}

% --- Bảng 8: job_skills ---
\subsubsection*{8. Bảng \texttt{job\_skills} (Yêu cầu kỹ năng chi tiết của việc làm)}

Cầu nối liên kết các yêu cầu kỹ năng chuẩn hóa thuộc một tin tuyển dụng cụ thể.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{job\_skills}.}
\label{tab:job_skills} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{job\_id} & bigint & PK, FK -> jobs(job\_id) ON DELETE CASCADE & ID tin tuyển dụng yêu cầu. \\
\hline
\texttt{skill\_id} & integer & PK, FK -> skills(skill\_id) ON DELETE CASCADE & ID kỹ năng chuẩn hóa. \\
\hline
\texttt{is\_inferred} & boolean & DEFAULT false & Kỹ năng được suy diễn gián tiếp. \\
\hline
\texttt{reason} & varchar(50) & NULL & Lý do liên kết (match tên, LLM trích xuất). \\
\hline
\texttt{model\_name} & varchar(100) & NULL & Tên mô hình AI dùng để liên kết. \\
\hline
\texttt{similarity\_score} & numeric(4,3) & NULL & Điểm tương đồng ngữ nghĩa khi đối sánh. \\
\hline
\texttt{lib\_version} & varchar(20) & NULL & Phiên bản thư viện sử dụng. \\
\hline
\texttt{raw\_skill\_name} & varchar(255) & NULL & Tên kỹ năng thô trong văn bản job. \\
\hline
\end{longtable}

% --- Bảng 9: cv_job_matches ---
\subsubsection*{9. Bảng \texttt{cv\_job\_matches} (Kết quả so khớp và phân tích khoảng cách năng lực)}

Lưu trữ kết quả tính toán độ tương thích giữa CV của sinh viên với một vị trí công việc cụ thể hoặc một nhóm ngành nghề.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{cv\_job\_matches}.}
\label{tab:cv_job_matches} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{match\_id} & uuid & PK, DEFAULT gen\_random\_uuid() & Định danh duy nhất của kết quả khớp. \\
\hline
\texttt{cv\_id} & uuid & FK -> user\_cvs(cv\_id) ON DELETE CASCADE & ID CV tham gia so khớp. \\
\hline
\texttt{job\_id} & bigint & FK -> jobs(job\_id) ON DELETE SET NULL, NULL & ID Job cụ thể tham gia so khớp (nếu có). \\
\hline
\texttt{match\_type} & varchar(50) & CHECK (match\_type IN ('search\_group', 'existing\_job', 'url\_job')) & Phân loại kiểu so khớp. \\
\hline
\texttt{search\_group} & varchar(100) & NULL & Tên nhóm ngành chuẩn hóa được so khớp. \\
\hline
\texttt{match\_score} & numeric(5,2) & CHECK (match\_score >= 0 AND match\_score <= 100) & Điểm số phần trăm tương thích tổng thể. \\
\hline
\texttt{radar\_data} & jsonb & NOT NULL & Cấu trúc JSON biểu diễn phân bố kỹ năng (vẽ biểu đồ Radar). \\
\hline
\texttt{gap\_report} & jsonb & NOT NULL & Chi tiết báo cáo khoảng cách kỹ năng. \\
\hline
\texttt{model\_version} & varchar(100) & NULL & Phiên bản thuật toán đối sánh. \\
\hline
\texttt{created\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm tính toán. \\
\hline
\texttt{updated\_at} & timestamp & DEFAULT CURRENT\_TIMESTAMP & Thời điểm cập nhật kết quả. \\
\hline
\end{longtable}

% --- Bảng 10: job_group_skill_weights ---
\subsubsection*{10. Bảng \texttt{job\_group\_skill\_weights} (Trọng số kỹ năng theo nhóm ngành)}

Bảng lưu trữ trọng số tầm quan trọng $w_i$ của từng kỹ năng thuộc một nhóm ngành công việc tiêu chuẩn, được tính toán bằng thuật toán TF-IDF trên tập dữ liệu việc làm.

\begin{longtable}{|L{2.8cm}|L{2.8cm}|L{3.5cm}|L{4.5cm}|}
\caption{Cấu trúc bảng \texttt{job\_group\_skill\_weights}.}
\label{tab:skill_weights} \\
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endfirsthead
\hline
\textbf{Tên trường} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ý nghĩa} \\
\hline
\endhead
\texttt{search\_group} & varchar(100) & PK & Tên nhóm ngành công việc tiêu chuẩn. \\
\hline
\texttt{skill\_id} & integer & PK, FK -> skills(skill\_id) ON DELETE CASCADE & ID kỹ năng chuẩn hóa trong nhóm. \\
\hline
\texttt{weight\_wi} & numeric(8,4) & NOT NULL & Trọng số đặc trưng $w_i$ của kỹ năng trong nhóm. \\
\hline
\end{longtable}

% ================================================================
\section{Thiết kế giao diện}

% ----------------------------------------------------------------
\subsection{Trang chủ và Dashboard thống kê thị trường việc làm}

Hệ thống CareerNova được thiết kế với hai điểm vào chính cho người dùng: trang chủ (Landing Page) dành cho khách chưa xác thực và trang Dashboard thị trường dành cho người dùng đã đăng nhập.

\textbf{Trang chủ (Landing Page)} là giao diện đầu tiên người dùng tiếp cận khi truy cập hệ thống. Phần đầu trang (hero section) trình bày thông điệp giá trị cốt lõi của nền tảng cùng hai nút hành động chính: \textit{"Bắt đầu miễn phí"} (dẫn đến trang đăng ký) và \textit{"Xem Thông tin Thị trường"} (dẫn đến Dashboard). Tiếp theo, bốn tính năng chủ đạo được trình bày dưới dạng lưới (so khớp CV, đề xuất việc làm, phân tích kỹ năng, dashboard thị trường) kèm luồng sử dụng bốn bước trực quan để định hướng người dùng mới.

\begin{figure}[H]
    \centering
    % TODO: Chụp trang career-nova.online (chưa đăng nhập) → lưu vào images/ui_landing.png
    \includegraphics[width=0.95\textwidth]{ui_landing}
    \caption{Giao diện trang chủ (Landing Page) của hệ thống CareerNova.}
    \label{fig:ui_landing}
\end{figure}

\textbf{Dashboard thống kê thị trường} (route \texttt{/dashboard}) là trang phân tích thị trường trung tâm, được tổ chức theo cấu trúc thông tin từ tổng quan đến chi tiết. Phần trên cùng hiển thị thanh lọc ba chiều (địa điểm, khoảng thời gian, loại công việc) và bốn thẻ thống kê tóm tắt: số tin tuyển dụng đang mở, mức lương IT trung bình (min/max/avg), số công ty đang tuyển và tỷ lệ tăng trưởng thị trường. Khu vực biểu đồ chính bao gồm ba thành phần bố trí theo hai hàng:

\begin{itemize}
    \item \textbf{AreaChart "Xu hướng tin tuyển dụng"} (chiếm 2/3 chiều rộng): biểu diễn tổng số tin tuyển dụng (đường xanh dương) và số vị trí làm việc từ xa (đường xanh lá) theo thời gian, với trục X là nhãn ngày/tháng và trục Y là số lượng tin.
    \item \textbf{PieChart "Phân bổ thị trường"} (1/3 chiều rộng): biểu đồ tròn thể hiện tỷ trọng công việc phân theo ngành, kèm chú thích màu sắc cho từng danh mục.
    \item \textbf{BarChart "Mức Lương theo Vị Trí"} (toàn bộ chiều rộng): biểu đồ cột đôi song song hiển thị mức lương tối thiểu (cột xanh nhạt) và tối đa (cột xanh đậm) cho từng chức danh công việc, đơn vị nghìn USD/năm.
\end{itemize}

Phía dưới các biểu đồ, danh sách \textit{Hot Jobs} (top công việc được lưu nhiều nhất trong 7 ngày) và hai lưới kỹ năng (\textit{Top 10 Skills} và \textit{Rising Skills}) cung cấp thông tin thị trường chi tiết theo từng vị trí. Luồng tương tác tổng thể của người dùng với Dashboard được mô tả trong Hình~\ref{fig:ui_dashboard_flow}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ui_dashboard_flow}
    \caption{Sơ đồ hoạt động mô tả luồng tương tác người dùng với Dashboard thị trường.}
    \label{fig:ui_dashboard_flow}
\end{figure}

\begin{figure}[H]
    \centering
    % TODO: Đăng nhập vào career-nova.online → vào /dashboard → scroll thấy đủ 3 biểu đồ → chụp → lưu vào images/ui_market_dashboard.png
    \includegraphics[width=0.95\textwidth]{ui_market_dashboard}
    \caption{Giao diện Dashboard thống kê thị trường việc làm IT, thể hiện AreaChart xu hướng tuyển dụng, PieChart phân bổ ngành và BarChart dải lương.}
    \label{fig:ui_market_dashboard}
\end{figure}

% ----------------------------------------------------------------
\subsection{Trang quản lý CV và kết quả so khớp năng lực}

Luồng phân tích năng lực của người dùng trong hệ thống CareerNova được chia thành hai trang kế tiếp nhau: trang CV Matching (tải lên và so khớp) và trang Skill Gap Analysis (phân tích khoảng cách kỹ năng chi tiết).

\textbf{Trang CV Matching} (route \texttt{/cv-matching}) được bố cục theo dạng hai cột. Cột trái hiển thị CV hiện đang được chọn làm mặc định (được đánh dấu bằng viền màu xanh và badge \textit{"Active CV"}) cùng danh sách dropdown để chuyển đổi giữa các CV đã tải lên. Cột phải là vùng tải lên CV mới với giao diện kéo-thả (drag-and-drop), chấp nhận định dạng PDF, JPG và PNG với giới hạn kích thước 5MB, kèm phản hồi trực quan tức thì (spinner trong khi tải, dấu tích khi thành công, thông báo lỗi khi không hợp lệ). Phía dưới hai cột là bộ chọn nguồn so khớp (nhóm tin tuyển dụng hoặc URL cụ thể) và nút \textit{"Phân tích \& So khớp"}.

Sau khi hệ thống hoàn tất xử lý, kết quả được hiển thị ngay trên cùng trang với ba thành phần: vòng tròn điểm Match Score được tô màu theo ngưỡng (xanh lá $\geq$80\%, xanh dương $\geq$65\%, vàng $<$65\%); ba thẻ kỹ năng có thể mở rộng phân loại theo màu sắc tương phản (Matched --- xanh lá, Partial --- vàng, Missing --- đỏ); và biểu đồ Radar nhỏ so sánh trực tiếp kỹ năng CV với yêu cầu JD.

\begin{figure}[H]
    \centering
    % TODO: Vào career-nova.online/cv-matching → tải CV lên → so khớp → chờ kết quả → chụp màn hình thấy cả phần upload và kết quả → lưu vào images/ui_cv_matching.png
    \includegraphics[width=0.95\textwidth]{ui_cv_matching}
    \caption{Giao diện trang CV Matching: cột trái quản lý CV, cột phải vùng tải lên, kết quả so khớp với Match Score và phân loại kỹ năng.}
    \label{fig:ui_cv_matching}
\end{figure}

\textbf{Trang Skill Gap Analysis} (route \texttt{/skill-gap}) là giao diện phân tích năng lực chuyên sâu, được tổ chức theo bốn tầng từ tổng quan đến chi tiết. Tầng đầu tiên là hai thẻ tóm tắt: \textit{"Mức độ phù hợp với thị trường"} (phần trăm tổng thể) và \textit{"Khoảng trống nghiêm trọng"} (số kỹ năng ưu tiên cần bổ sung). Tầng thứ hai trình bày biểu đồ Radar \textit{"Chi tiết kỹ năng"} với hai vùng tô chồng nhau: vùng xanh dương thể hiện điểm kỹ năng thực tế của người dùng (\texttt{user\_score}) và vùng xanh lá mờ thể hiện ngưỡng yêu cầu thị trường (\texttt{market\_score}); tooltip tương tác hiển thị giá trị phần trăm của từng kỹ năng khi di chuột. Tầng thứ ba là Gap Report theo danh mục kỹ năng, mỗi danh mục được trình bày với biểu tượng emoji đặc trưng, badge điểm khoảng cách và thanh tiến độ đôi so sánh \texttt{user\_rate} với \texttt{market\_rate}. Khi mở rộng từng danh mục, từng kỹ năng con hiển thị badge trạng thái ba màu (Matched/Partial/Missing) cùng điểm số chi tiết. Tầng thứ tư là phần Learning Paths, gợi ý các khóa học và lộ trình học tập có thể mở rộng để xem từng bước chi tiết.

Luồng tương tác đầy đủ của người dùng từ bước tải CV đến xem kết quả phân tích được mô tả trong Hình~\ref{fig:ui_cv_flow}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ui_cv_flow}
    \caption{Sơ đồ hoạt động mô tả luồng tương tác từ tải lên CV đến xem kết quả Skill Gap Analysis.}
    \label{fig:ui_cv_flow}
\end{figure}

\begin{figure}[H]
    \centering
    % TODO: Vào career-nova.online/skill-gap → chờ load xong → chụp màn hình thấy RadarChart + Gap Report (có thể mở rộng 1-2 danh mục) → lưu vào images/ui_skill_gap.png
    \includegraphics[width=0.95\textwidth]{ui_skill_gap}
    \caption{Giao diện trang Skill Gap Analysis: biểu đồ Radar so khớp kỹ năng và Gap Report phân loại kỹ năng theo danh mục.}
    \label{fig:ui_skill_gap}
\end{figure}

% ================================================================
\section{Thiết kế thuật toán phân tích khoảng cách năng lực}

Thuật toán phân tích khoảng cách năng lực (Gap Analysis Algorithm) là nhân tố cốt lõi giúp sinh viên nhận thức được vị trí năng lực hiện tại của bản thân so với yêu cầu thị trường lao động. Hệ thống thực hiện việc này thông qua việc so khớp các kỹ năng trích xuất từ CV của người dùng với bộ kỹ năng định hình cho từng vị trí công việc hoặc nhóm ngành.

% ----------------------------------------------------------------
\subsection{Luồng xử lý từ lúc người dùng upload CV đến khi nhận kết quả so khớp}

Quy trình xử lý của hệ thống từ khi sinh viên tải tệp CV lên cho đến khi nhận được kết quả phân tích khoảng cách năng lực được mô tả theo các bước dưới đây.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{image3_5_1}
    \caption{Sơ đồ luồng xử lý từ khi người dùng tải CV lên đến khi nhận kết quả so khớp.}
    \label{fig:cv_flow}
\end{figure}

\textbf{Bước 1 --- Trích xuất văn bản từ CV (Text Extraction):}
Hệ thống hỗ trợ tải lên CV dạng PDF hoặc hình ảnh (.png, .jpg, .jpeg). Thư viện \texttt{pdfplumber} được sử dụng để trích xuất trực tiếp lớp chữ nếu tệp PDF có định dạng số văn bản (Digital PDF). Nếu tệp PDF là dạng scan ảnh hoặc người dùng upload tệp ảnh trực tiếp, hệ thống kích hoạt luồng OCR dự phòng sử dụng thư viện \texttt{pdf2image} để chuyển trang PDF sang ảnh, sau đó kết hợp \texttt{pytesseract} (Tesseract OCR) để đọc chữ từ ảnh.

\textbf{Bước 2 --- Tối ưu hóa bằng cơ chế Cache:}
Sau khi trích xuất văn bản thô, hệ thống thực hiện chuẩn hóa khoảng trắng của văn bản và truy vấn so khớp trên bảng \texttt{public.user\_cvs} của người dùng. Nếu tìm thấy bản ghi có nội dung văn bản trùng khớp hoàn toàn (Cache Hit), hệ thống bỏ qua việc gọi API Gemini và bước chuẩn hóa kỹ năng, thay vào đó nạp trực tiếp dữ liệu kỹ năng từ bảng cầu nối \texttt{public.user\_cv\_skills}, giúp tăng tốc độ phản hồi và tiết kiệm tài nguyên API.

\textbf{Bước 3 --- Trích xuất kỹ năng bằng Generative AI (Skill Extraction):}
Trong trường hợp Cache Miss, hệ thống sử dụng mô hình ngôn ngữ lớn Gemini thông qua API để trích xuất các kỹ năng xuất hiện trên CV. Prompt gửi đi yêu cầu mô hình trả về cấu trúc dữ liệu JSON chứa: tên kỹ năng, dẫn chứng ngữ cảnh xuất hiện (evidence) và mức độ tin cậy (confidence score $\geq 0.85$).

\textit{Cơ chế chống ảo giác (Anti-Hallucination Validation):} Do LLM đôi khi sinh ra các kỹ năng không có thực trong CV, hệ thống thực hiện kiểm tra chéo: mọi kỹ năng trích xuất phải có tên hoặc dẫn chứng xuất hiện trực tiếp dưới dạng chuỗi con (substring) trong văn bản CV gốc. Nếu không thỏa mãn, kỹ năng đó sẽ bị hủy bỏ.

\textit{Luồng xử lý dự phòng (Fallback):} Nếu kết nối LLM gặp sự cố hoặc cạn kiệt hạn ngạch (quota limit), hệ thống tự động kích hoạt bộ đối sánh từ khóa thô (Keyword Match Fallback) trực tiếp với toàn bộ từ điển trong bảng \texttt{public.skills} bằng các biểu thức chính quy (Regex) thiết lập ranh giới từ để trích xuất kỹ năng tạm thời.

\textbf{Bước 4 --- Chuẩn hóa kỹ năng sang từ điển hệ thống (Skill Normalization):}
Các kỹ năng thô sau trích xuất được đưa qua quy trình chuẩn hóa Lightcast Taxonomy kết hợp mô hình embedding ngữ nghĩa \texttt{all-MiniLM-L6-v2}. Quy trình gồm 4 tầng xử lý:

\begin{itemize}
    \item \textbf{Tầng 0 (Dọn dẹp phổ quát):} Loại bỏ ký tự đặc biệt, chuẩn hóa chữ thường, xử lý viết tắt (ví dụ: \texttt{css3} thành \texttt{CSS}, chuẩn hóa framework \texttt{.js}).
    \item \textbf{Tầng 1 (Khớp chính xác):} So khớp trực tiếp chuỗi ký tự với từ điển canonical và tên rút gọn của bảng \texttt{public.skills}.
    \item \textbf{Tầng 2 (Tìm kiếm ngữ nghĩa ngữ cảnh):} Chuyển đổi tên kỹ năng và thông tin bổ trợ thành vector embedding ngữ cảnh. Sử dụng công cụ FAISS để tìm kiếm Top 5 ứng viên kỹ năng có độ tương đồng cosine cao nhất từ bộ nhớ đệm cache (\texttt{skills\_embedding.pkl}).
    \item \textbf{Tầng 3 (Xử lý lai \& Phân loại):} Tính điểm lai kết hợp 85\% Cosine Similarity và 15\% sparse Jaccard n-gram (độ tương đồng ký tự) để hạn chế sai lệch từ vựng. Điểm số được áp dụng phạt nếu loại kỹ năng (Hard Skill vs Common Skill) không trùng khớp, sau đó đối sánh với ngưỡng động (Dynamic Threshold) phù hợp với loại từ (từ viết tắt cần độ khớp $\geq 0.80$, kỹ năng chuyên môn cần $\geq 0.70$).
\end{itemize}

Các kỹ năng được chuẩn hóa thành công sẽ được gán với \texttt{skill\_id} tương ứng trong cơ sở dữ liệu và lưu trữ vào bảng cầu nối \texttt{public.user\_cv\_skills}.

\textbf{Bước 5 --- Tính toán so khớp năng lực (Matching Engine):}
Chương trình nạp bộ kỹ năng yêu cầu và trọng số tương ứng từ bảng \texttt{public.job\_group\_skill\_weights} cho Job/Nhóm ngành mục tiêu, sau đó tính toán độ tương đồng giữa các kỹ năng CV của sinh viên với các kỹ năng của Job, phân loại khoảng cách và tính toán chỉ số tương thích tổng thể (Match Score).

\textbf{Bước 6 --- Lưu trữ và kết xuất kết quả:}
Kết quả phân tích phân nhóm kỹ năng, điểm tương thích cùng các báo cáo thiếu hụt được lưu trữ vào bảng \texttt{public.cv\_job\_matches} dưới dạng JSONB để phục vụ vẽ biểu đồ Radar trên giao diện người dùng.

% ----------------------------------------------------------------
\subsection{Thuật toán so sánh tập kỹ năng trong CV với yêu cầu của Job để tính tỷ lệ tương thích}

Để đánh giá mức độ tương thích giữa hồ sơ năng lực của ứng viên (tập kỹ năng đã chuẩn hóa từ CV) với yêu cầu tuyển dụng (tập kỹ năng đặc trưng kèm trọng số của Job), hệ thống triển khai thuật toán tính toán độ tương đồng ngữ nghĩa có trọng số (Weighted Semantic Similarity) dựa trên không gian vector embedding.

\subsubsection*{Các định nghĩa toán học}

\begin{itemize}
    \item Gọi $\mathcal{J} = \{j_1, j_2, \ldots, j_M\}$ là tập hợp gồm $M$ kỹ năng yêu cầu của vị trí công việc.
    \item Gọi $\mathcal{W} = \{w_1, w_2, \ldots, w_M\}$ là tập các trọng số tương ứng của các kỹ năng trong $\mathcal{J}$ đối với công việc đó ($w_i > 0$), lấy từ bảng \texttt{public.job\_group\_skill\_weights}. Trọng số này phản ánh mức độ quan trọng của kỹ năng đối với vị trí nghề nghiệp.
    \item Gọi $\mathcal{C} = \{c_1, c_2, \ldots, c_K\}$ là tập hợp gồm $K$ kỹ năng được trích xuất và chuẩn hóa thành công từ CV của sinh viên.
    \item Với mỗi kỹ năng trong cơ sở dữ liệu, vector embedding phân tán 384 chiều được sinh ra bởi mô hình \texttt{all-MiniLM-L6-v2}: $\mathbf{e}_{j_i}$ đại diện cho biểu diễn ngữ nghĩa của kỹ năng $j_i$.
\end{itemize}

\subsubsection*{Công thức tính toán chi tiết}

\textbf{Bước 1 --- Tính độ tương thích ngữ nghĩa lớn nhất cho từng kỹ năng yêu cầu:}

Đối với mỗi kỹ năng yêu cầu $j_i$, hệ thống duyệt qua toàn bộ tập kỹ năng có sẵn của sinh viên $\mathcal{C}$ để tìm độ tương đồng Cosine lớn nhất:
\begin{equation}
    \text{sim}_i = \max_{c_k \in \mathcal{C}} \, \text{cosine}(\mathbf{e}_{j_i},\, \mathbf{e}_{c_k})
\end{equation}

Vì các vector embedding trong cache đã được chuẩn hóa L2 ($\|\mathbf{e}\|_2 = 1$), độ tương đồng Cosine được tính nhanh bằng tích vô hướng giữa hai vector:
\begin{equation}
    \text{sim}_i = \max_{c_k \in \mathcal{C}} \left( \hat{\mathbf{e}}_{j_i} \cdot \hat{\mathbf{e}}_{c_k} \right)
\end{equation}

Giá trị $\text{sim}_i$ nằm trong khoảng $[0, 1]$ (mọi giá trị âm đều được quy về $0$).

\textbf{Bước 2 --- Tính đóng góp thực tế và khoảng cách thiếu hụt:}

Đóng góp (Contribution) của kỹ năng thứ $i$ vào tổng điểm năng lực của ứng viên:
\begin{equation}
    \text{contrib}_i = w_i \times \text{sim}_i
\end{equation}

Khoảng cách thiếu hụt (Gap) của kỹ năng thứ $i$:
\begin{equation}
    \text{gap}_i = w_i \times (1 - \text{sim}_i)
\end{equation}

\textbf{Bước 3 --- Tính điểm số tương thích tổng hợp (Match Score):}

Điểm số tương thích tổng thể là tổng đóng góp của tất cả các kỹ năng yêu cầu, được chuẩn hóa chia cho tổng trọng số:
\begin{equation}
    \text{MatchScore} = \frac{\displaystyle\sum_{i=1}^{M} w_i \times \text{sim}_i}{\displaystyle\sum_{i=1}^{M} w_i}
\end{equation}

\textbf{Bước 4 --- Chuyển đổi thành tỷ lệ phần trăm (Match Percent):}

Tỷ lệ tương thích hiển thị trên giao diện người dùng được tính bằng:
\begin{equation}
    \text{MatchPercent} = \text{MatchScore} \times 100\%
\end{equation}

\subsubsection*{Cơ chế phân loại kỹ năng phục vụ báo cáo khoảng cách (Gap Report)}

Hệ thống sử dụng các ngưỡng điểm tương đồng ngữ nghĩa lớn nhất ($\text{sim}_i$) để phân loại từng kỹ năng yêu cầu $j_i$ vào ba nhóm hiển thị trên giao diện người dùng:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{image3_5_2}
    \caption{Quy tắc phân loại kỹ năng phục vụ báo cáo khoảng cách năng lực.}
    \label{fig:skill_classification}
\end{figure}

\begin{enumerate}
    \item \textbf{Kỹ năng đã có (Possessed/Matched Skills):}
    \begin{itemize}
        \item \textit{Điều kiện:} $\text{sim}_i \geq 0.75$
        \item \textit{Ý nghĩa:} Ứng viên đã sở hữu kỹ năng này trực tiếp hoặc thông qua một kỹ năng tương đương rất gần về ngữ nghĩa (ví dụ: yêu cầu ``Scrum'' và CV có ``Agile/Scrum'').
        \item \textit{Thông tin đi kèm:} Ghi nhận tên kỹ năng đối sánh gián tiếp (\texttt{matched\_via}) nếu không trùng ID trực tiếp.
    \end{itemize}

    \item \textbf{Kỹ năng cần bổ sung thêm (Partially Matched Skills):}
    \begin{itemize}
        \item \textit{Điều kiện:} $0.40 \leq \text{sim}_i < 0.75$
        \item \textit{Ý nghĩa:} Ứng viên có kỹ năng liên quan nhưng chưa hoàn toàn đáp ứng đúng ngữ cảnh yêu cầu của Job (ví dụ: yêu cầu ``Consulting'' nhưng CV mới có ``Project Design''). Đây là các mục tiêu đào tạo ngắn hạn, ứng viên có thể tự cải thiện nhanh.
    \end{itemize}

    \item \textbf{Kỹ năng thiếu hoàn toàn (Missing Skills):}
    \begin{itemize}
        \item \textit{Điều kiện:} $\text{sim}_i < 0.40$
        \item \textit{Ý nghĩa:} Ứng viên thiếu hụt hoàn toàn kiến thức này. Đây là lỗ hổng năng lực lớn nhất, đòi hỏi lộ trình học tập dài hạn hơn để bù đắp nếu muốn ứng tuyển vào vị trí công việc tương ứng.
    \end{itemize}
\end{enumerate}
